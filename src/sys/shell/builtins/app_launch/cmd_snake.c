// src/sys/shell/builtins/app_launch/cmd_snake.c — the `snake` shell command.
// Extracted verbatim from the former monolithic src/sys/shell.c.
#include "../../shell_internal.h"

void cmd_snake(void) {
start_ular();
}
