# Mectov OS

[![CI Boot Test](https://github.com/MAliffadlan/Mectov_OS/actions/workflows/build-boot-test.yml/badge.svg)](https://github.com/MAliffadlan/Mectov_OS/actions/workflows/build-boot-test.yml)

A monolithic x86 kernel written from scratch in C and Assembly — no external libraries, no libc, no POSIX. Every byte runs directly on hardware: GRUB Multiboot boot, protected mode with paging, a graphical desktop with a floating-window manager, a real Unix process model, and an SMP scheduler that runs user tasks on all four CPU cores.

---

## Table of Contents

- [Overview](#overview)
- [Highlights](#highlights)
- [Architecture](#architecture)
- [Project Structure](#project-structure)
- [Core Subsystems](#core-subsystems)
- [Syscall API Reference](#syscall-api-reference)
- [Applications](#applications)
- [Build and Run](#build-and-run)
- [Testing](#testing)
- [Version History](#version-history)
- [License](#license)

---

## Overview

Mectov OS is a from-scratch i386 operating system with:

- **Ring 0 / Ring 3 isolation** — user applications run in separate address spaces with COW fork, demand-paged `mmap()`, and per-process page directories — on **PAE 3-level paging with NX (W^X)** since v38.49: user heap/stack/mmap pages are non-executable, and an instruction-fetch fault on data kills the task with SIGSEGV (`nxtest` proves it in CI).
- **True SMP** — per-CPU runqueues, work-stealing migration, and a LAPIC timer per core; all four QEMU cores execute tasks (verified live by SysInfo's per-core load bars).
- **A Unix process model** — `fork` / `exec` / `waitpid`, POSIX signals (`sigaction`, `sa_mask`, `SA_RESTART`), process groups, sessions and a controlling terminal with SIGTTIN/SIGTTOU, plus System V shared memory, pipes, and fds.
- **Ownership, permissions & real threads (v38.23–v38.24, v38.27)** — every file carries a POSIX uid/gid + 9-bit mode enforced at the syscall layer (`chmod`/`chown`/`ls -l`, root bypass, ext2-persisted); every task has a private `%gs` TLS descriptor with `clone()` + a pthread-style runtime, so thread-local variables are genuinely per-thread; futex-based `mct_mutex_t`/`mct_cond_t` (pthread-style) are proven on 4 cores by `conddemo`; and **POSIX resource limits (v38.28)** — `RLIMIT_NPROC`/`RLIMIT_AS`/`RLIMIT_NOFILE` per task with soft/hard values, POSIX `getrlimit`/`setrlimit` privilege rules, and enforcement at fork/clone/thread-create, fd allocation, and heap/mmap growth, so a fork bomb or an fd/memory leak can no longer take the whole system down.
- **A real shell (msh)** — env vars with `$VAR` expansion, aliases, tab completion, scripts, job control, and a growing POSIX toolkit (`uname`, `whoami`, `hostname`, `env`, `seq`, `head`, `grep`, `cp`, `mv`, `df`, ...).
- **A graphical desktop** — double-buffered window manager (resize, Aero snap, rounded corners), taskbar with system tray and clickable window buttons (minimize/restore, hover-highlighted, v38.39), a Start menu with search-as-you-type filtering (v38.40), draggable persistent icons with gradient tiles, soft shadows and label chips (v38.46), and a graphical login screen.
- **Eager FPU/SSE context switching (v38.41)** — every task carries a 512-byte `fxsave` image the scheduler swaps on every context switch, so DOOM's x87 and any Ring 3 float/SSE code can be preempted against each other safely (proven by `fputest`: two forked processes accumulating in live x87/SSE registers).
- **Runtime mounts & POSIX sockets (v38.42–v38.43)** — a real mount table with `mount`/`umount` (syscalls + shell builtins, root-only), and an fd-integrated socket API (`socket`/`bind`/`listen`/`accept`/`connect`/`sendto`/`recvfrom`) where accept spawns per-connection slots POSIX-style and read/write/poll/select work on socket fds uniformly.
- **Real ACPI poweroff (v38.45)** — the FADT is parsed for the PM1a/b control blocks and the DSDT `\_S5` package, so `shutdown` writes the firmware's own sleeping type + SLP_EN; QEMU exits by itself (`poweroff_test` asserts it).
- **On-disk persistence** — an ATA-backed VFS, a fully writable ext2 partition (verified with host `fsck.ext2`), a FAT32 volume at `/fat32` (drive 3, LFN read/write), an **AHCI/SATA driver (v38.50)** that registers SATA disks as drives 4+ and an **xHCI/USB 3.0 driver (v38.56)** that registers SuperSpeed sticks as drives 8+ on the same sector API — ext2/FAT32 and runtime `mount()` work on SATA/USB volumes unchanged.
- **Networking** — RTL8139 driver with IRQ-driven RX, 16-connection TCP with a listener/backlog accept model, UDP, ARP/ICMP/DNS, and a browser that reaches the real internet through a host gateway proxy.
- **Audio** — Sound Blaster 16 driver with WAV playback and PC-speaker tones.
- **Developer tooling** — in-kernel GDB stub (attach over TCP, breakpoints, single-step), ELF32 + custom `.mct` loaders, and a shared-library runtime that keeps app binaries under 2 KB.

---

## Highlights

**FPU context switching, runtime mounts, POSIX sockets & ACPI S5 (v38.41–v38.45)**
- **Eager fxsave/fxrstor (v38.41)**: `task_t` grew a 16-byte-aligned 512-byte fxsave image; `schedule()` swaps it with IF=0 under `task_lock` (SMP-safe, no per-CPU owner tracking). Fresh tasks are seeded from a canonical `fninit` template, `fork()` inherits the parent's live state, `exec()` resets it (POSIX). CR4.OSFXSR/OSXMMEXCPT are enabled per-CPU (BSP + every AP); #NM is a logged safety net and #XM delivers `SIGFPE` to Ring 3. `apps/fputest.c` + `scripts/fputest.py` prove two forked processes accumulating in live x87-stack and SSE registers across preemption both stay exact — the exact scenario the old "single FPU owner" design could not survive.
- **Runtime mount table (v38.42)**: `SYS_MOUNT`/`SYS_UMOUNT` (root-only) + `mount`/`umount` shell builtins over a mount-point table; the boot `/ext2` and `/fat32` mounts are its first two entries, `umount` drops the subtree and restores a plain directory, `mount` re-inits the backend and re-populates from the real disk. `scripts/mount_test.py` does a full umount → refuse-non-mount → remount → fat32demo-PASSES roundtrip.
- **POSIX socket API (v38.43)**: `socket`/`bind`/`listen`/`accept`/`connect`/`sendto`/`recvfrom` (syscalls 108–114) return ordinary fds — `read`/`write`/`close`/`poll`/`select` are socket-aware. The listener model is POSIX-shaped: a LISTEN slot never mutates; each inbound SYN spawns a fresh child slot (free slots = backlog) and `accept()` hands completed children out one by one. `socktest.py` verifies a guest→host client echo (10.0.2.2) AND a host→guest server accept (hostfwd) end to end with PING/PONG.
- **Capacity bumps (v38.44)**: TCP connections 8→16, global fd table 128→256 (socket-heavy servers), windows 16→32. `MAX_TASKS` stays 64 (GDT TLS coupling) and `MAX_NODES` 256 (on-disk format), both documented in code.
- **ACPI S5 (v38.45)**: `acpi_init` now records the FADT in addition to the MADT (both XSDT and RSDT paths scan every entry), parses the DSDT `\_S5` package for the real SLP_TYP values, and `shutdown` writes PM1a/PM1b with SLP_EN before falling back to the legacy hardcoded ports. `poweroff_test.py` asserts the QEMU process exits by itself.

**Scheduling & SMP (v36.7–v36.8)**
- Per-CPU runqueues: each core picks with priority + aging, steals work from idle peers (migration), and parks in a pinned Ring 0 idle task.
- LAPIC timer per Application Processor, PIT-calibrated once on the BSP (concurrent calibration corrupted the shared PIT and caused timer storms).
- Every runqueue mutation under one `task_lock`, cli-first — the timer IRQ can never self-deadlock in `schedule()`.
- Per-CPU load sampling exposed to Ring 3: SysInfo renders four live utilization bars; the serial log prints `[LOAD] c0=.. c1=..` every 3 s.
- `smpstress` forks 8 children (CPU burn + sleep + pipes + self-signals) in two parallel waves across all four cores — all 16 exit and are reaped.

**Process model & signals (v36.1–v36.6, v38.5–v38.7)**
- COW `fork()` with deterministic syscall-frame copying (the KVM fork-GPF fix), in-place `exec()` via live-frame patching, blocking `waitpid()` with WNOHANG.
- POSIX signals with `SIGACTION`/`SIGPROCMASK`, handler sigframes on the user stack, `SA_RESTART` re-parking interrupted sleeps — and unresolvable Ring 3 faults deliver `SIGSEGV` to user handlers instead of killing the task.
- Process groups + sessions + controlling terminal: `setpgid`, `setsid`, `tcsetpgrp`; background readers get stopped by SIGTTIN; Ctrl+C/Ctrl+Z target the foreground group.
- Real pipelines and redirection: `run A | run B`, `run A > file`, `run A < file` through dup2 + a per-task fd table.
- Job control: Ctrl+Z stops, `jobs` lists, `bg` resumes, `fg` waits, `kill %1` terminates.
- Demand paging for heap and stack: a shared lazy zero page + COW backs untouched pages, and an unmapped guard page catches stack overflow cleanly.

**Shell & toolkit (v36.9, v37.x)**
- POSIX-style builtins across three toolkit rounds: `uname`, `whoami`, `hostname`, `env`, `seq`, `head`, `wc`, `type`, `cat -n`, `yes`, `printf`, `sort`, `uniq`, `tee`, `find` — all pipe-aware.
- Shell scripting: `for VAR in list`, `while true/false`, `$VAR` expansion, `break`, 4-level nesting; env vars + aliases + tab completion.
- `passwd` stores the login password in `/etc/passwd` as `<salt_hex>:<sha256>` since v38.52 (single source, no hardcoded default, no plaintext on disk); a single `OS_VERSION` constant keeps the help banner, `mfetch`, `uname` and login footer in sync.

**Memory & kernel hardening (v38.3–v38.8)**
- Frame-bitmap physical allocator sized from the multiboot memory map (RAM above 128 MB usable; identity map to 512 MB), per-process address spaces, COW with frame refcounting, demand-paged `mmap()` (`0x40000000..0x80000000`), System V shared memory.
- File-backed `mmap()` (`SYS_MMAP_FILE` + `SYS_MSYNC`): pages fault in from disk on demand, dirty pages write back on msync/munmap, `fork()` shares them MAP_SHARED-style.
- 16 KB kernel stacks with a 4 KB guard page below each; vector 8 is a hardware task gate so an overflow prints `[PANIC] DOUBLE FAULT` instead of triple-faulting into a silent reboot.
- Lock audit: per-structure irqsave spinlocks replaced global `cli()` — syscalls are preemptible and SMP parallelizes; heap gets magic + canary + redzone, clean panic on double-free, OOM returns NULL.
- `dmesg` — a 64 KB in-memory kernel log ring (crash lines included) exposed via `dmesg` and `/proc/dmesg`.

**Filesystems & storage (v37.0, v38.12, v38.15–v38.16, v38.23, v38.25)**
- Persistent VFS with auto-save; a fully writable ext2 partition (host-verified `fsck.ext2` clean); a **FAT32 volume at `/fat32` (drive 3) with long file names (LFN) read AND written**, interoperable with host `mkfs.fat`/mtools both ways.
- **Bus-mastering IDE DMA (v38.26)**: the PIIX3 IDE controller's BMIDE engine moves the data itself through a Physical Region Descriptor table — the CPU programs the PRDT + command and waits, no port I/O in the data path at all. Under KVM a 160 KB uncached read dropped **~42 ms → ~1.3 ms (~31×)**, and ~510× vs the original per-sector PIO (~680 ms); `scripts/bigread_test.py` now asserts DMA was engaged and no transfer failed. (v38.25 added multi-sector PIO batches + `rep insw`/`rep outsw` as the no-DMA fallback.)
- POSIX file positioning: `SYS_LSEEK` (`SEEK_SET/CUR/END`), `SYS_FSTAT`, honored `O_APPEND`; fd reads advance a real offset.
- **Ownership & permissions (v38.23)**: every VFS node carries a POSIX uid/gid + 9-bit mode, enforced at open/read/write/create/delete/exec in the syscall layer — root (uid 0) bypasses, `chmod` needs ownership, `chown` is root-only, and chmod while a descriptor is open takes effect on the *next* read/write. `SYS_CHMOD`/`SYS_CHOWN` (+ shell `chmod`/`chown` builtins and `ls -l`), persisted to ext2 inodes so permissions survive the image. FAT32//proc//dev keep no permission concept.
- Virtual `/proc` generated live from kernel state: `/proc/tasks`, `/proc/meminfo`, `/proc/cpuinfo`, `/proc/uptime`, `/proc/version`, `/proc/dmesg`.

**Network (v35.5, v38.11, v38.43)**
- 16-slot multi-connection TCP with seq/ack, retransmit/timeout sweeps, a full FIN handshake, and a listener/backlog `accept()` model (the listener never becomes the connection — each inbound SYN gets its own slot); UDP bind/send/recv (fd-integrated datagram sockets, single global binding); DNS via the QEMU gateway.
- **DHCP client (RFC 2131)** configures IP/netmask/gateway/DNS at runtime over UDP broadcast, with static fallback (`ipconfig` prints the result); RTL8139 IRQ-driven RX (spinlock-guarded against the SMP TX race).

**GUI & input (v37.x–v38.x)**
- Double-buffered WM: dirty-region tracking, Aero snap, 8-directional resize, traffic-light controls, scroll-wheel events (IntelliMouse), 32-window capacity (v38.44), shared `ps2_drain()` arbitration so keyboard and mouse never steal each other's bytes.
- Instrument-console identity: a Windows-style lock screen → password login, live RTC clock. The whole chrome is **full black** (v38.37) — taskbar with neutral bevels (no warm/orange tint), a plain centered **"START"** label instead of the amber logo square, and **window chrome** (titlebars + borders) pure black with off-white title text and window-control glyphs (no amber hairline around focused windows); amber survives only as selection feedback inside the Start menu/calendar popups.
- **Login gate (v38.30–v38.33)**: full-bleed scaled wallpaper (never cropped), digital clock + full date (day, date, month, year) pinned bottom-left, SPACE-only password entry, and a 4-second idle fallback back to the clock view. The lock screen is deliberately bare — no branding, no hint, no version footer (v38.33). While the password panel is open (v38.31) the wallpaper **blurs** and every caption **disappears**, so the panel is the only thing in focus. Every switch between the two screens **cross-fades** (v38.32): the blur eases in/out, the captions fade, and the panel fades — one lerp pass animates all of it, in both directions.
- **Lock screen feature (v38.34)**: the login gate is no longer only a boot/logout screen — a **Lock** item in the Start menu, the **Ctrl+Alt+L** shortcut, and the `lock` shell builtin all run the gate **without resetting the session**: every window and task stays alive behind the lock (the old text-mode `lock` stub printed "System Locked" and verified nothing; it's now a flag the kernel main loop polls, `pending_lock`). Unlocking with the password restores the desktop via `full_redraw()` — verified pixel-identical, terminal window included. **Idle auto-lock (v38.51)**: `locktimeout <seconds>` arms a countdown that locks the same way once keyboard+mouse have been quiet that long (`0` disables); the countdown restarts on any input and is re-anchored after unlock so the desktop never re-locks immediately.
- **Kernel CSPRNG + hardened passwords (v38.52)**: a ChaCha8 DRBG (`src/sys/entropy.c`) is seeded at boot and fed continuously from the timer IRQ + every keyboard/mouse byte; `/dev/random` and TCP ISNs now draw from it, a new `SYS_GETRANDOM` syscall (117) exposes it to Ring 3, and `passwd` writes a salted SHA-256 hash (`salt:hash`) instead of plaintext — legacy plaintext files still verify until the next change.
- **Ring 3 scanout takeover — the display-server foundation (v38.53)**: `SYS_FB_MAP` (118) maps the VBE linear framebuffer read/write into a Ring 3 task as `PAGE_DEV` PTEs (never COW'd on fork, never refcounted or freed by the frame allocator, not inherited by children), authorized logind-style — uid 0 or the controlling terminal's foreground process group; while the holder lives, **the kernel desktop stops presenting frames entirely** and keystrokes pump to it through the same queue `SYS_GET_KEY` pops (`SYS_FB_RELEASE` (119), exit or exec hands the pixels back and the desktop repaints). The clone/free page-table walkers skip `PAGE_DEV` entries — closing a latent unbounded-refcount OOB on MMIO addresses fork would have hit. `apps/fbmap.mct` animates straight to the scanout, then chases the mouse until any key releases; `scripts/fbmap_test.py` proves both the clean-release roundtrip AND a Ctrl+C kill mid-takeover with the desktop restored.
- Smooth compositing: delta-copy VRAM presentation (~10× less MMIO traffic), truthful FPS HUD, tick-scaled 60 FPS cap — <1 ms/frame on KVM.

**App runtime & tooling (v38.3, v38.13, v38.24, v38.27)**
- `.mct` binary format + standard ELF32 loader + shared-library runtime (app binaries ~2 KB); the first **Rust** Ring 3 app (`apps/rusthello.rs`, `no_std`, same MCT1 format) proves the loader is language-agnostic.
- **Real per-thread TLS + `clone()` (v38.24)**: every task slot owns a private FS/GS GDT descriptor (DPL=3) whose base is the thread's control block, saved/restored with the context-switch frame in all three interrupt stubs — so `%gs:0` is a genuine per-thread pointer that survives preemption, signals, fork and CPU migration. `SYS_CLONE` (child stack + TLS base) creates threads sharing the caller's address space; `SYS_TLS_SET` installs/removes the current thread's TLS; `apps/lib/thread.h` gives apps a tiny pthread-style runtime (`mct_tls_init`/`mct_thread_create`/`mct_thread_join`), and `threaddemo` proves isolation — 4 cloned workers each bump a private counter in their own TCB, every one landing on exactly `ITERS`.
- **Futex mutexes + condition variables (v38.27)**: `apps/lib/sync.h` gives apps pthread-style `mct_mutex_t`/`mct_cond_t` on top of `SYS_FUTEX_WAIT`/`SYS_FUTEX_WAKE` — the mutex fast path is one `lock cmpxchgl`, the slow path parks the thread in the kernel (`TASK_STATE_BLOCKED`, burns no CPU, works across cores); the condvar keeps a sequence counter so a signal racing the sleep is never lost, and the kernel's `futex_wait` re-checks the value under its lock. `apps/conddemo.mct` proves both on 4 cores: 4 threads bump a shared counter to exactly 20,000 under the mutex, then 2 producers + 2 consumers move 3,000 items through a bounded buffer over the condvar with no loss, no duplicates, and zero `[PANIC]`. Making it reliable exposed a real scheduler wake-path bug (see the v38.27 changelog).
- The 3,384-line shell monolith split into a dispatcher + 72 per-command modules; in-kernel GDB stub (COM2, breakpoints, single-step, DWARF symbols).

---

## Architecture

```
+--------------------------------------------------------------------+
|                       GRUB Multiboot (VBE)                         |
+--------------------------------------------------------------------+
|  boot.asm  -->  kernel.c  (kernel_main entry point)                |
+--------------------------------------------------------------------+
|  GDT (Ring 0+3)  |  IDT (ISR+IRQ)  |  TSS  |  Syscall (int 0x80)   |
+--------------------------------------------------------------------+
|  PIT Timer (1kHz, BSP) |  LAPIC Timer (per AP)  |  PS/2  |  Serial |
+--------------------------------------------------------------------+
|  Memory Manager  |  VMM (per-process page dirs, COW, mmap, shm)    |
+--------------------------------------------------------------------+
|  ACPI (RSDT/XSDT/MADT)  |  SMP (APIC/IOAPIC, 4 cores)              |
+--------------------------------------------------------------------+
|  Per-CPU Runqueues  |  Semaphores & Futexes  |  VFS + ATA PIO + AHCI + xHCI USB |
+--------------------------------------------------------------------+
|  VGA/VESA Driver   |  Window Manager  |  RTL8139 NIC (IRQ-driven)  |
+--------------------------------------------------------------------+
|  Network Stack (Ethernet/ARP/IPv4/ICMP/UDP/TCP/DNS)                |
+--------------------------------------------------------------------+
|  MCT + ELF32 Loaders |  Ring 3 User Tasks |  GDB Stub (COM2)       |
+--------------------------------------------------------------------+
|  Desktop |  Taskbar  |  Login Screen  |  fd layer  |  UNIX pipes   |
+--------------------------------------------------------------------+
```

### Target Platform

| Component | Detail |
|---|---|
| CPU | i386 (32-bit x86), monolithic kernel |
| Privilege | Ring 0 (kernel) + Ring 3 (user) |
| Scheduler | Preemptive, per-CPU runqueues, priority + aging, work stealing |
| Display | VESA VBE linear framebuffer, 1024×768, 32-bit color |
| Storage | ATA PIO (IDE) — drive 0 VFS, drive 1 ext2, drive 3 FAT32 (drive 2 CD-ROM) · AHCI SATA 4+ · xHCI USB 3.0 8+ |
| Audio | Sound Blaster 16 + PC speaker |
| Network | RTL8139 (virtual, via QEMU) |
| Serial | COM1/COM2 UART 16550A (38400 baud, 8N1) |

---

## Project Structure

```
boot.asm               Multiboot header + 32-bit bootstrap
kernel.c               kernel_main: init order, main loop, input dispatch
linker.ld / Makefile   Kernel link script + parallel build (target-isolated)
run.sh                 QEMU launcher (KVM, 4 cores, gateway proxy, serial log)

src/
  sys/                 Kernel core
    task.c             Scheduler: per-CPU runqueues, fork/exec/waitpid/signals
    schedule, sleep/wake, per-CPU load sampling
    vmm.c              Frame allocator, page dirs, COW, mmap, shm frames
    loader.c           MCT + ELF32 loaders, loader_build_image/task_exec
    syscall.c/.gui/.vfs/.net/.ipc/.proc   Modular syscall handlers
    shell.c            Kernel shell (commands, env vars, aliases, jobs)
    smp.c apic.c acpi.c  AP boot (INIT-SIPI-SIPI), APIC/IOAPIC, MADT
    ext2.c vfs.c fd.c  ext2 write layer, VFS, file descriptors, pipes
    sync.c ipc.c shm.c clipboard.c security.c  Sync primitives, IPC, shm
    gdt.c idt.c timer.c interrupt_entry.asm  CPU setup, IRQ dispatch, 1 kHz tick
  drivers/
    vga.c font8x16.c   Framebuffer compositor, primitives, font
    keyboard.c mouse.c serial.c rtc.c timer.c speaker.c
    ata.c ahci.c xhci.c pci.c net.c rtl8139.c sb16.c dma.c gdb_stub.c
  gui/
    wm.c desktop.c taskbar.c login.c   Window manager + desktop shell
  include/             Public headers (syscall.h, task.h, vmm.h, ...)

apps/                  Ring 3 user applications
  terminal.c nano.c notepad.c explorer.c sysinfo.c taskmgr.c browser.c fbmap.c
  clock.c calc.c gcalc.c snake.c flappy.c pci.c mplayer.c volume.c
  hello.c elfdemo.c syncdemo.elf udptest.elf    (ELF32 demos)
  forkdemo.c execdemo.c shmdemo.c mmapdemo.c mmapfiledemo.c lseekfiledemo.c
  sigdemo.c bgread.c smpstress.c pipegen.c piperead.c tcpserver.c looper.c
  rusthello.rs               (FIRST RING 3 APP IN RUST — freestanding no_std,
                             built by scripts/build_rust_mct.py, same MCT1
                             format the C apps use; rustc ≥ stable via rustup,
                             target i686-unknown-uefi, entry resolved at link)
  lib/libc.c libc.h    Homegrown dynamic shared library (libc.mct)

scripts/               build_mct.py, build_elf.py, inject_vfs.py, gateway.py,
                       boot_test.py, usb_test.py, ahci_test.py, fbmap_test.py, debug.py (serial log reader)
docs/architecture/     scheduler.md, memory.md, syscalls.md, smp_and_apic.md
.github/workflows/     build-boot-test.yml (CI boot test)
doom/                  Embedded DOOM engine (GPLv2)
```

---

## Core Subsystems

### Kernel & Memory

| Subsystem | Highlights |
|---|---|
| Virtual Memory (`vmm.c`) | Frame-bitmap allocator (32 768 pages), per-process page dirs, region allocator, COW with refcounts |
| Demand paging | `mmap()` reserves zero frames; the PF handler lazily maps+zeroes one page per fault |
| Shared memory | System V segments, `PAGE_SHARED` skips COW, leak-proof attach bitmap |
| Syscall layer | Modular handlers, every user pointer validated (`validate_user_ptr`) |
| GDT/IDT/TSS | Ring 0/3 segments, interrupt-gate IRQs, trap-gate `int 0x80` (preemptible syscalls) |
| Debugging | In-kernel GDB stub (RSP on COM2): F12 break, breakpoints, single-step, DWARF symbols |

### Scheduling & SMP (`task.c`, `smp.c`, `apic.c`)

- Per-CPU runqueues with priority (IDLE < LOW < NORMAL < HIGH) + aging; work-stealing migration between cores.
- APs tick via their own LAPIC timer (PIT-calibrated once on the BSP before AP wake — prevents timer storms).
- One `task_lock`, cli-first everywhere; signal delivery on the syscall-return path is locked too.
- Task states: FREE / RUNNING / READY / SLEEP / BLOCKED / STOPPED / ZOMBIE; hlt-park for zero-CPU waits.
- Per-CPU load sampling → `SYS_GET_SYSINFO` (`cpu_count`, `cpu_load[4]`) → SysInfo live bars.
- 16 KB kernel + 64 KB user stacks per task; per-CPU TSS with kernel-stack switching.

### Process Model (`syscall_proc.c`, `loader.c`)

| Feature | Notes |
|---|---|
| `fork()` | COW address-space clone, kernel-stack frame copy from `TSS.esp0 - sizeof(registers_t)` (deterministic), fd/handler/mmap/shm inheritance |
| `exec()` | Live syscall-frame patching; caught handlers reset to default, pending signals cleared (POSIX) |
| `waitpid()` | Blocks in TASK_STATE_BLOCKED, reaps zombie, WNOHANG; 15 s orphan-zombie safety net |
| Signals | sigaction/sa_mask/sigprocmask/SA_RESTART; sigframes on user stack; default action = exit `128+sig` |
| Job control | pgrp/session per task; SIGTTIN/SIGTTOU on background tty access; fg/bg/jobs/kill %n |
| Shell | env vars + `$VAR`, aliases, tab completion, scripts, `ps`/`kill`/`uptime`/`memstat`, `fetch`; POSIX toolkit `uname`/`whoami`/`hostname`/`env`/`seq`/`head` |

### Filesystems & Storage

- **VFS** — 16-slot node table over ATA PIO, auto-save, growth guard (files move to the end of disk instead of overwriting neighbors).
- **ext2** — fully writable secondary partition: block/inode bitmap allocators, file grow/truncate (direct + singly-indirect), directory entry create/remove/rename with slack-splitting, superblock sync; allocators refuse metadata blocks; host `fsck.ext2` clean.
- **FAT32** — third drive (secondary slave, `index=3`), mounted at `/fat32`: BPB parsing, FAT cluster-chain traversal, **long file names (LFN) read + written** (reverse-order chain, checksum-verified; SFN fallback), cluster allocation/free on write/create/delete, whole-tree rebuild at mount; volumes created by host `mkfs.fat`/mtools work both ways.
- **FD layer** — 16 fds/task, 128 global, typed (`FILE`/`PIPE_READ`/`PIPE_WRITE`/`DEV`), refcounted, closed on task teardown.

### Network

- RTL8139 driver (bus mastering, IRQ 11 → INT 43), spinlock-serialized TX/RX (SMP race fix).
- Full in-kernel stack: Ethernet, ARP, IPv4, ICMP, UDP, DNS (via QEMU gateway `10.0.2.3`).
- Multi-connection TCP: 8 slots, per-conn seq/ack + 16 KB RX buffer, retransmit (5×6 s) + connect timeout (10 s), FIN handshake.
- UDP bind/send/recv syscalls; `fetch [domain]`, `ping`, `host` shell commands; host `gateway.py` proxy for real-internet HTTP.

### GUI & Input

- Double-buffered WM: dirty-region tracking, z-order, rounded corners, traffic-light buttons, Aero snap, 8-directional resize (min 220×150), minimized-window ghost prevention.
- Desktop: baked wallpaper, squircle icons, draggable + persistent positions, double-click launch.
- Taskbar: per-app icon buttons, WiFi indicator, CAPS + HDD LEDs, WIB clock; system tray.
- Input: IntelliMouse 4-byte protocol + scroll wheel (event type 4), shared `ps2_drain()` 8042 arbitration, modifier-key tracking (Alt+Tab).
- Rendering: triple-buffer delta compositing (~4 ms/frame), enforced clip rectangle on all primitives.

### Audio

- SB16 native driver, 8/16-bit mono/stereo WAV streaming, tone synthesis, volume control (scroll-adjust), mapped to QEMU's PulseAudio backend.

### App Runtime

- `.mct` binary format (16-byte header, fixed-base mapping) + standard ELF32 ET_EXEC loader (auto-detected by magic).
- Shared-library system (`libc.mct`): dynamic symbol resolution via export-table pointer; app binaries ~2 KB.
- `SYS_SET_STDOUT_IPC` lets apps stream output to queues; pipelines/redirection via `task_fork_exec` + dup2 rewiring.

---

## Syscall API Reference

All syscalls are invoked via `int 0x80` (trap gate). Registers: `EAX` = number, `EBX`/`ECX`/`EDX`/`ESI`/`EDI` = arguments.

### Core (1–10)
| # | Name | Description |
|---|------|-------------|
| 1 | SYS_PRINT | Print string. EBX=str_ptr, ECX=color |
| 2 | SYS_OPEN | Open/create VFS file. EBX=filename → fd |
| 3 | SYS_READ | Read file. EBX=fd, ECX=buf, EDX=size |
| 4 | SYS_WRITE | Write file/pipe/serial. EBX=fd, ECX=buf, EDX=size |
| 5 | SYS_CLOSE | Close fd. EBX=fd |
| 6–7 | SYS_MALLOC / SYS_FREE | Identity-mapped heap allocate/free |
| 8 | SYS_GET_TICKS | PIT tick count |
| 9 | SYS_YIELD | Yield CPU (`sti; hlt; cli`) |
| 10 | SYS_EXIT | Terminate current task |

### GUI (11–17)
| # | Name | Description |
|---|------|-------------|
| 11 | SYS_DRAW_RECT | EBX=win_id, ECX=x, EDX=y, ESI=(w<<16)\|h, EDI=color |
| 12 | SYS_DRAW_TEXT | EBX=win_id, ECX=x, EDX=y, ESI=str, EDI=color |
| 13 | SYS_GET_KEY | Non-blocking keyboard char |
| 14 | SYS_GET_MOUSE | Mouse state → EAX=x, EBX=y, ECX=buttons |
| 15 | SYS_CREATE_WINDOW | EBX=x, ECX=y, EDX=w, ESI=h, EDI=title → win_id |
| 16 | SYS_GET_EVENT | Window event (paint/key/mouse/scroll/close) |
| 17 | SYS_UPDATE_WINDOW | Commit draw commands |

### Threads & Processes (18–22, 46–51)
| # | Name | Description |
|---|------|-------------|
| 18 | SYS_THREAD_CREATE | EBX=entry, ECX=priority, EDX=page_dir → TID |
| 19 | SYS_SLEEP | Sleep EBX ticks |
| 20–22 | SYS_GET_PID / SET_PRIORITY / GET_PRIORITY | Task identity + priority |
| 46 | SYS_GET_TASKS | List tasks. EBX=array, ECX=max |
| 47 | SYS_GET_WINDOWS | List windows. EBX=array, ECX=max |
| 48 | SYS_KILL_TASK | Force kill EBX=tid |
| 49 | SYS_GET_LAUNCH_ARG | Launch argument string |
| 50 | SYS_CREATE_FILE | Create empty VFS file |
| 51 | SYS_LOAD_LIBRARY | Dynamically load shared library → base |

### IPC (23–28)
| # | Name | Description |
|---|------|-------------|
| 23–26 | SYS_IPC_CREATE / SEND / RECV / DESTROY | Named 64-byte message queues |
| 27–28 | SYS_IPC_TRY_SEND / TRY_RECV | Non-blocking variants |

### Memory & VM (29–31, 78–83, 93–94)
| # | Name | Description |
|---|------|-------------|
| 29–31 | SYS_VMM_MAP / ALLOC / FREE | Low-level page mapping |
| 78 | SYS_SHMGET | Create/attach segment. EBX=key, ECX=size → id |
| 79–81 | SYS_SHMAT / SHMDT / SHMCTL | Attach / detach / IPC_RMID |
| 82 | SYS_MMAP | Reserve VA range (lazy zero-fault). EBX=size → base |
| 83 | SYS_MUNMAP | Free faulted frames of a range |
| 93 | SYS_MMAP_FILE | Map an open VFS file fd; pages fault in lazily **from disk**; dirty pages write back on msync/munmap. EBX=fd, ECX=flags |
| 94 | SYS_MSYNC | Flush dirty pages of a file-backed mapping back to the file |
| 95 | SYS_LSEEK | Reposition fd read/write offset. EBX=fd, ECX=offset, EDX=whence(SEEK_SET/CUR/END) → new offset |
| 96 | SYS_FSTAT | File metadata by fd. EBX=fd, ECX=stat_t* {size,type,node_idx,parent,data_sector,name} |

### I/O Multiplexing & POSIX misc (97–101)
| # | Name | Description |
|---|------|-------------|
| 97 | SYS_POLL | POSIX poll(). EBX=pollfd_t*, ECX=nfds, EDX=timeout_ms (<0 = forever) → ready count |
| 98 | SYS_SELECT | POSIX select(). EBX=nfds, ECX=readfds*, EDX=writefds*, ESI=exceptfds*, EDI=timeout_ms (uint32_t bitmaps) → ready count |
| 99 | SYS_GETCWD | Absolute path of the working directory. EBX=buf, ECX=size → 0 |
| 100 | SYS_CHDIR | Change working directory. EBX=path (relative or absolute) → 0 |
| 101 | SYS_CLOCK_GETTIME | Monotonic uptime. EBX=CLOCK_MONOTONIC, ECX=timespec_t* {tv_sec,tv_nsec} → 0 |

### UNIX Compatibility & Hardware (32–38)
| # | Name | Description |
|---|------|-------------|
| 32 | SYS_PIPE | Create pipe pair. EBX=pipefd[2] |
| 33 | SYS_GET_TIME | RTC time. EBX=rtc_time_t* |
| 34 | SYS_PLAY_SOUND | Tone. EBX=freq, ECX=ms |
| 35 | SYS_GET_SYSINFO | Hardware + stats incl. per-core load |
| 36–38 | SYS_GET_PCI_INFO / LIST_DIR / STAT_FILE | PCI, directory, file info |

### Network (39–43, 67–70)
| # | Name | Description |
|---|------|-------------|
| 39 | SYS_DNS_RESOLVE | Resolve domain |
| 40 | SYS_TCP_CONNECT | EBX=ip, ECX=port → conn id 0–7 |
| 41–42 | SYS_TCP_SEND / RECV | EDX=conn_id |
| 43 | SYS_NET_STATUS | Packed network state |
| 67–69 | SYS_UDP_BIND / SEND / RECV | Datagram sockets |
| 70 | SYS_TCP_CLOSE | Graceful FIN shutdown. EBX=conn_id |
| 85 | SYS_TCP_LISTEN | Listen on a port (TCP server) |

### Synchronization (61–66)
| # | Name | Description |
|---|------|-------------|
| 61–64 | SYS_SEM_CREATE / WAIT / POST / DESTROY | Counting semaphores, FIFO waiters |
| 65 | SYS_FUTEX_WAIT | Sleep while *addr==expected. EBX=addr, ECX=expected |
| 66 | SYS_FUTEX_WAKE | EBX=addr, ECX=max_waiters → woken |

### Process Model & Signals (71–77, 84, 86–92)
| # | Name | Description |
|---|------|-------------|
| 71 | SYS_FORK | COW clone → child tid / 0 / -1 |
| 72 | SYS_WAITPID | EBX=pid, ECX=status, EDX=WNOHANG |
| 73 | SYS_KILL | Send signal. EBX=pid, ECX=sig |
| 74 | SYS_SIGNAL | Install handler (0 default, 1 ignore) |
| 75 | SYS_SIGRETURN | Restore pre-handler frame |
| 76–77 | SYS_GETPPID / SYS_EXEC | Parent id / replace image |
| 84 | SYS_DUP2 | Redirect an fd (pipelines/redirection) |
| 86 | SYS_SIGACTION | Full sigaction: handler, sa_mask, flags |
| 87 | SYS_SIGPROCMASK | Block/unblock signals |
| 88–92 | SYS_SETPGID / GETPGRP / SETSID / TCSETPGRP / TCGETPGRP | Process groups, sessions, controlling terminal |
| 102–103 | SYS_CHMOD / SYS_CHOWN | Change permission bits (owner/root) / transfer ownership (root only) |
| 104–105 | SYS_CLONE / SYS_TLS_SET | Create thread (child stack + TLS base; child sees 0) / install current thread's TLS (`%gs` → TCB) |

---

## Applications

| App | Type | Description |
|---|---|---|
| Terminal | .mct | Terminal emulator: history, tab completion, auto-suggest, scrollback, job control; POSIX builtins `uname`/`env`/`seq`/`head`/... |
| Nano / Notepad | .mct | Text editors with auto-save and dialogs |
| File Explorer | .mct | Browse/open files, CRUD, context menus, file associations |
| SysInfo | .mct | Live RAM/CPU/uptime + **per-core load bars** |
| Task Manager | .mct | Task/window list, kill processes |
| PCI Manager | .mct | Scrollable PCI device table |
| Music Player | .mct | SB16 WAV streaming |
| Volume Control | .mct | Scroll-adjustable system volume |
| Clock | .mct | WIB digital clock |
| Snake / Flappy | .mct | Games in WM windows |
| Mini Browser | .mct | Text-mode web browser via gateway proxy |
| Calculator | .mct | GUI calculator |
| DOOM | kernel | Full 1993 DOOM engine embedded in the kernel — runs **windowed in a WM window** (`doom`), `doom -fullscreen` keeps the legacy framebuffer grab |
| ELF Demo | .elf | Proves the ELF32 loader |
| Sync Demo | .elf | Semaphore + futex workers |
| UDP Test | .elf | UDP datagram round-trip |
| Fork Demo | .mct | COW fork + SIGUSR1 handler, exit 42 |
| Exec Demo | .mct | fork → exec different image → exit 7 |
| SHM Demo | .mct | Two processes share a segment |
| mmap Demo | .mct | Sparse 1 MiB mapping, lazy zero-fill |
| mmap File Demo | .mct | File-backed mmap: fault pages in from disk, write back via msync/munmap |
| lseek File Demo | .mct | SYS_LSEEK/SYS_FSTAT/O_APPEND on real files |
| Poll/Select Demo | .mct | POSIX poll()/select() over fds+pipes, getcwd/chdir, clock_gettime |
| FAT32 Demo | .mct | FAT32 drive 3: read mtools files, create/write/append/delete in `/fat32` |
| Sig Demo | .mct | sigaction/mask/SA_RESTART semantics |
| BG Read | .mct | Background-group reader stopped by SIGTTIN |
| SMP Stress | .mct | 8 children × 2 waves across 4 cores |
| Pipe Gen/Read | .mct | Pipeline producer/consumer |
| TCP Server | .mct | Echo server (host-connectable) |
| Looper | .mct | Infinite loop (Ctrl+C testing) |
| Fuzz Syscall | .mct | ~2000 random syscalls with hostile args — kernel must never panic |
| IO Bench | .mct | Page-cache micro benchmark: cold disk read vs cached RAM read, repeated write timing |

---

## Build and Run

### Requirements
`gcc (-m32)` · `nasm` · `make` · `qemu-system-i386` · `python3` + `Pillow` · `grub-mkrescue` + `mtools` · `mkfs.ext2` (for the ext2 drive)

### Commands
```bash
make clean && make        # build kernel + apps + ISO assets
./run.sh                  # KVM boot: 4 cores, gateway proxy, serial -> serial_debug.log
```

`run.sh` boots `mectov.iso` with `-smp 4`, the RTL8139 NIC, SB16 audio, and three drives: `disk.img` (MECTOVFS, index 0), `ext2.img` (index 1) and `fat32.img` (a 16 MB FAT32 volume at index 3 — index 2 is the CD-ROM; created with `mkfs.fat` if missing), streaming the serial log to `serial_debug.log` (also copied to `log.txt`). The Web Gateway Proxy (`scripts/gateway.py`) gives the guest real internet access.

The whole toolchain builds optimized: the kernel and embedded DOOM compile at `-O2 -march=i686 -fno-pie -fno-pic` (no GOT/PIC indirection — the kernel is linked at a fixed 1 MB and never relocates), and every Ring 3 app, `libc.mct` and ELF demo builds at `-O2`.

### Building User Applications
```bash
make apps/hello.mct       # builds any apps/<name>.c -> apps/<name>.mct (baked into the VFS)
```
App binaries are produced by `scripts/build_mct.py` (freestanding, entry `_start`, linked at `0x08000000`) and injected into the VFS by the Makefile. Standard ELF32 binaries work too:
```bash
python3 scripts/build_elf.py apps/elfdemo.c elfdemo.elf
```

### Debugging with GDB
```bash
# Terminal 1 — boot with COM2 as a GDB server
qemu-system-i386 -cdrom mectov.iso -m 128 -smp 4 \
  -serial file:serial.log -serial tcp:127.0.0.1:2345,server=on,wait=off \
  -drive file=disk.img,format=raw,index=0,media=disk

# Terminal 2 — attach (press F12 inside the guest to break in)
gdb myos.bin -ex "target remote :2345" -ex "c"
```

---

## Testing

The project is tested on QEMU under **both KVM (4-core)** and TCG:

Run the whole battery locally with **`make check`** (builds the kernel + ISO, recreates fresh `disk.img`/`ext2.img`/`fat32.img` exactly like CI, runs every suite in CI order, and prints a concise pass/fail table in `.check/summary.txt`). **`make check-quick`** runs a fast high-signal subset (~6–8 min); `make check CHECK_ARGS=--keep-images` keeps the current disk images and `make check CHECK_ARGS=--only=<suite>` runs one suite. The fork/fputest KVM regressions are opt-in via `make check CHECK_ARGS=--kvm` — some hosts' KVM cannot run the `qemu32` machine even with `/dev/kvm` present (the guest stalls at AP wake); CI runs them on runners where KVM works.

| Suite | What it verifies |
|---|---|
| `scripts/boot_test.py` | Boot → login → `BOOTED KERNEL LOOP` smoke window |
| `scripts/fork_test.py` | COW fork + waitpid + signals (TCG) |
| `scripts/jobcontrol_test.py` | `sleep 2 &`, `jobs`, `bg`, `fg`, `kill %1` (TCG) |
| `scripts/fat32_test.py` | FAT32 drive 3: mtools-created files, nested dir, create/write/read-back, O_APPEND, delete (TCG) |
| `scripts/pollselect_test.py` | poll/select over files+pipes (incl. timeout, EOF/HUP, POLLNVAL), getcwd/chdir round-trip, clock_gettime monotonic (TCG) |
| `scripts/fuzz_test.py` | Ring 3 syscall fuzzer: ~2000 random+hostile syscalls (garbage ptrs/sizes/fds), targeted overflow/sigreturn/kill probes, zero `[PANIC]` (TCG) |
| `scripts/iocache_test.py` | Whole-file page cache: cold disk read vs cached reads (min-of-N, must beat 3x), same-size rewrite save-skip, zero `[PANIC]` (TCG) |
| `scripts/app_smoke_test.py` | Demo apps, one boot: `smpstress` (8 children across 4-core SMP: CPU burn + sleep + self-signals → `ALL PASS`), `sigdemo` (sigprocmask/sa_mask/SA_NODEFER/SA_RESTART), `crashme` (deliberate `ud2` → kernel logs `[EXCEPTION] int_no=6` and survives) (TCG) |
| `scripts/doom_test.py` | Windowed DOOM: Terminal → `doom` → a WM window opens, the game loop enters, and `[DOOM] tick` lines prove frames are being produced and composited by the WM; OS stays alive (TCG; `--kvm` for speed) |
| `scripts/login_redesign_test.py` | Redesigned login gate: wallpaper full-bleed + amber clock at bottom-left, non-SPACE key ignored, SPACE opens the password panel through the fade/blur transition (waits for it to settle), wallpaper blurs + clock/footer hidden while typing, panel stays up, 4 s idle reverts through the closing fade (retries until the clock is back), password still logs in (TCG) |
| `scripts/mmapfile_test.py` | File-backed mmap: lazy page fault-in from disk, msync write-back, munmap, exit 0 (TCG) |
| `scripts/rusthello_test.py` | First Rust Ring 3 app (`rusthello.mct`): loads, runs, prints all markers, exits 0 (TCG) |
| `scripts/lseekfile_test.py` | POSIX lseek/fstat/O_APPEND on real files: all assertions pass, exit 0 (TCG) |
| `scripts/panic_test.py` | Multi-core panic self-test: boots with `panic_self_test panic=reboot` + `-no-reboot`, the kernel panics deliberately and must dump registers for **all 4 cores** (NMI-IPI snapshot), then reset so QEMU exits cleanly (TCG) |
| `scripts/perm_test.py` | Ownership/permission enforcement from Ring 3: create/write own file, chmod 0400/0000 (open-time + per-call read/write denials), chmod restore, chown root-only, root-owned 0644 read-allowed/write+delete-denied (TCG) |
| `scripts/thread_test.py` | TLS + `clone()`: main thread installs TLS, 4 cloned workers each with a private `%gs` TCB bump per-thread counters; every counter must land on exactly `ITERS` (isolation proof), joins via waitpid (TCG) |
| `scripts/cond_test.py` | Futex mutex + condvar: 4 threads bump a shared counter to exactly 20,000 under the mutex, then 2 producers + 2 consumers move 3,000 items through a bounded buffer over a condvar — every item consumed exactly once, zero `[PANIC]` (TCG, 4 cores) |
| `scripts/rlimit_test.py` | Resource limits: getrlimit defaults sane, fork refused under NPROC cur=1 (per-uid count), fork OK after raising cur, non-root may never raise the hard limit, NOFILE stops fd allocation at the soft limit (close frees a slot), AS stops mmap at the soft limit (2 MB under 1 MB refused, 256 KB allowed, raise re-allows) — zero `[PANIC]` (TCG) |
| `scripts/ulimit_test.py` | Shell `ulimit` builtin: `-a` lists all resources with sane defaults, `-n 8` lowers NOFILE and reads back 8/16, raising soft above the hard limit is refused for a non-root caller, `-u 4` lowers NPROC, raising cur back to the hard limit is allowed, and rlimittest still passes afterwards (limits are inherited by child processes) — zero `[PANIC]` (TCG) |
| `scripts/bigread_test.py` | IDE DMA + multi-sector disk reads: asserts the BMIDE controller engaged and zero DMA transfers failed, then a 160 KB uncached file read 3× with byte-pattern verification on every read; `--kvm` for real timing (TCG in CI) |
| `scripts/lock_test.py` | Lock screen feature: logs in, launches Terminal, locks via the `lock` builtin, unlocks and pixel-compares the terminal window pre/post (session preserved, not reset), then locks again via Ctrl+Alt+L (TCG) |
| `scripts/autolock_test.py` | Idle auto-lock: `locktimeout 20` arms the timer, a quiet desktop locks on its own, unlock restores the session without re-locking, `locktimeout 0` disables (a longer quiet window passes with no lock) (TCG) |
| `scripts/hardening_test.py` | Kernel hardening: `passwd mectov123 hunter2` then the NEW password unlocks while the OLD one is rejected (a plaintext file would accept both); `apps/hardening_test.mct` proves from Ring 3 that `SYS_GETRANDOM` returns fresh non-zero bytes, two `/dev/random` reads differ, and `/etc/passwd` is `salt:hash` with no plaintext — zero `[PANIC]` (TCG) |
| `scripts/ahci_test.py` | AHCI/SATA driver (ich9-ahci + drive 4): boots with `ich9-ahci` + FAT32 SATA disk, asserts `AHCI` `drive 4` + `[AHCI] ready`, `mount /sata fat32 4` (BPB via AHCI), `cp` + host `mtools` byte-compare (TCG) |
| `scripts/usb_test.py` | xHCI/USB 3.0 driver (qemu-xhci + drive 8): boots with `qemu-xhci` + FAT32 USB stick on SuperSpeed bus, asserts `USB3 -> drive 8` + `[XHCI] ready`, `mount /usb fat32 8` (BPB via xHCI), `cp` + host `mtools` byte-compare (TCG) |
| `scripts/fbmap_test.py` | Ring 3 scanout takeover: `SYS_FB_MAP` grants the framebuffer to a Ring 3 task, the kernel desktop suppresses its rendering, a typed key routes through the pump and releases cleanly (desktop restored), then a second takeover is killed with Ctrl+C mid-flight and ownership drops via task_cleanup with the desktop restored — zero `[PANIC]` (TCG) |
| KVM regressions | fork, exec, pipe, redir, mmap, shm, sig, ctrlc, stop, job, bgread, tcp, smpstress (4 cores) |

CI (`.github/workflows/build-boot-test.yml`) runs every suite above on each push: boot, login (redesigned gate), lock screen, idle auto-lock, kernel hardening (CSPRNG + salted passwd), Ring 3 scanout takeover, fork, jobcontrol, KVM fork, FAT32, poll/select, fuzz, page cache, app smoke tests, windowed DOOM, mmap, Rust, lseek, panic, permissions, TLS/clone, futex/condvar, resource limits, the `ulimit` shell builtin, and multi-sector PIO.

---

## Version History

| Version | Highlights |
|---|---|
| v38.60 | **USB HID class driver — xHCI keyboard + mouse (boot protocol), with PS/2 as the fallback.** The xHCI driver (v38.56 mass storage) now also classifies each port's device: HID boot interfaces (`03/01/01` keyboard / `03/01/02` mouse) get their interrupt IN endpoint configured (EP ctx type 7, Interval exponent in dw0 bits 16..23 from `bInterval` — QEMU asserts it nonzero), `SET_PROTOCOL(1)` boot protocol, and a single Normal-IN IOC TRB kept armed on a per-device ring (re-armed on every completion event; a NAKed transfer stays pending inside QEMU, so one TRB in flight is the right depth and bursty input queues in the device instead of being lost). Reports are translated into the exact byte stream the PS/2 path would produce: the 8-byte keyboard report (modifiers + up to 6 key usages) is diffed against the previous report and fed as set-1 make/break scancodes through the new `kbd_feed_scancode()` (same modifier tracking, feed-time mods snapshot, entropy feed), with L/R modifier bits collapsed and a usage→set-1 table covering letters, digits, punctuation, F1–F12, nav and keypad; the mouse report (buttons/dx/dy/wheel, HID axes: +y is down) lands in `mouse_hid_report()` with the same clamps/scroll semantics. Every consumer — terminal, login gate, WM, DOOM, `SYS_GET_KEY`, auto-lock — works unchanged, and PS/2 remains the fallback when no USB HID device exists (no-op fast-out in `xhci_hid_poll`, called once per main-loop and login-gate iteration; the gate blocks the main loop at boot/lock/logout so it must drain itself). A HID report landing mid-bulk-transfer is dispatched by `event_wait`'s discard path, so none is ever lost; the device pool grows to `USB_MAX_DRIVES + USB_MAX_HID` slots with a `xhci_drive_slot[]` ordinal map so a keyboard on a lower-numbered port can never displace a drive number. QEMU routing made the test genuine: `sendkey`/`mouse_move` go to the first ACTIVE handler, and the usb-kbd/usb-mouse become active on their first interrupt-IN poll — ahead of PS/2. One real bug found at bring-up: the Transfer Event length field is the *residue* (0 for a full-max-packet IN transfer), so every report was read as zero bytes until bytes-received = `mps − residue`. `scripts/usb_test.py` now attaches `usb-kbd` + `usb-mouse` behind `qemu-xhci` and asserts both enumerate, `[HID] kbd first report` (login typed through the USB keyboard), and `[HID] mouse first report` (cursor motion through xHCI — probe verified the cursor lands at the exact accumulated delta), while the USB3 storage round-trip still passes on the same controller; boot_test + app_smoke green (PS/2 fallback unaffected). Known bounds, same scope as storage: FS/LS devices and report-protocol HID (subclass 0) are logged and skipped. `OS_VERSION` bumped to 38.60. |
| v38.59 | **Local CI-parity test runner — `make check` — and the first release tag.** `scripts/check.py` mirrors `.github/workflows/build-boot-test.yml` suite-for-suite (same order, same `--timeout` values, same fresh `disk.img`/`ext2.img`/`fat32.img`), so a green local run is the same signal as a green CI run without the 23-minute GitHub round trip. New Makefile targets: `check` (full battery), `check-quick` (11-suite subset, ~6–8 min) and `iso` (kernel → `grub-mkrescue`); per-suite output lands in `.check/<suite>.log` with a concise pass/fail table + wall time saved to `.check/summary.txt`; `CHECK_ARGS=` passes extra flags through (`--only=<suite>`, `--keep-images`, `--kvm`). The fork/fputest **KVM regressions are opt-in** — `/dev/kvm` presence is not enough: this dev host's KVM cannot run the `qemu32,+nx` machine (the guest stalls at `[SMP] Waking up CPU APIC ID 0x1`, AP wake never completes — the same quirk as the earlier `doom --kvm` hang) while CI runners execute the same binary green, so CI stays authoritative for KVM. First full local run of the final runner: **33/33 TCG suites PASS in 23m 58s (exit 0)** — boot, login, lock, autolock, hardening, fbmap, taskbar hover, startmenu search, fork, jobcontrol, fputest, nxtest, fat32, mount, ahci, usb, socktest, poweroff, pollselect, fuzz, iocache, app_smoke, doom, mmapfile, rusthello, lseekfile, panic, perm, thread, cond, rlimit, ulimit, bigread. Also: annotated **git tag `v38.58`** now marks the CI-validated v38.58 release commit. `OS_VERSION` bumped to 38.59. |
| v38.58 | **Full toolchain optimization + the two latent bugs it exposed, and xHCI audit hardening (bulk-DCI bound + trace gating).** (1) **Userland builds at `-O2`** — every `.mct` app, `libc.mct` and the ELF demos had compiled at `-O0` since day one; `build_mct.py`/`build_lib.py`/`build_elf.py` now pass `-O2`. That switch instantly exposed a latent bug `-O0` had masked: the `int 0x80` wrappers in `src/include/syscall.h` carried **no `"memory"` clobber**, so GCC legally assumed the kernel never touches memory — `waitpid` exit-status reads were constant-folded to 0 (every child looked like exit 0 → `smpstress` FAIL) and the `sigaction` struct initializer was dead-store-eliminated (the kernel iret'd into a garbage handler → SIGSEGV → children silently killed). `"memory"` is now declared on `syscall()`, `syscall5()` and all 31 raw-asm wrappers, and `smpstress`'s CPU-burn accumulator went `volatile` so the loop can't be optimized away. (2) **The kernel was being compiled PIC without anyone knowing** — Ubuntu gcc's `-m32` defaults to `-fPIE` even for `-c`, so a kernel linked at a fixed 1 MB by `linker.ld` (never relocated, ever) shipped a `.got`/`.got.plt` and ~730 `__x86.get_pc_thunk` call sites; `-fno-pie -fno-pic` + pinned `-march=i686` (kernel + embedded DOOM) remove the indirection entirely — 0 `.got` sections, 0 thunks. LTO was tried and **rejected on measurement**: 17.8 fps vs 21.8 fps fitted DOOM frame rate under TCG, +560 KB binary, and two workarounds (a `__divdi3` carve-out for `doom_libc.o`, and `-Wl,--build-id=none` because the gcc driver's `.note.gnu.build-id` pushed `.text` — and the multiboot header with it — to file offset 8192, the outer edge of the bootloader's 8 KB scan window, so the kernel never booted). (3) **xHCI audit fixes** (`src/drivers/xhci.c`): `usb_enumerate` now validates `DCI=2*ep+dir` against the 32-byte input/output context arrays — a device with bulk EP ≥ 4 IN would have written 32-byte contexts past `ictx`/`dctx` into `ep0_ring` and let the xHC DMA-read past the buffer on real hardware (QEMU's usb-storage uses EP1/EP2, so it never tripped); such devices are now logged and skipped. And the per-event serial trace that printed one line per CBW/data/CSW transfer on the **data path** (73 lines for a small copy — polled serial throttling real USB I/O) is now gated to bring-up/enumeration only: 16 trace lines at boot, 0 after `[XHCI] ready`. Verified: boot/app_smoke/doom/fuzz/thread/cond/usb suites all green after every stage; in-guest `smpstress`'s 8-child wave finished in **335 ms vs ~2.8 s at `-O0` (~8×)**; wall-clock TCG suites improved −2%…−16% (emulation-dominated — the desktop apps spend most of their time in syscalls and kernel rendering, so real-hardware wins are larger than TCG wall clocks suggest). `OS_VERSION` bumped to 38.58. |
| v38.57 | **Merge Ring 3 scanout takeover (v38.53) + USB 3.0 (v38.56) + docs sync.** Reconciled the divergent `main` (local `v38.54..56` vs origin `v38.53` `SYS_FB_MAP`/`PAGE_DEV`): `loader.c` now carries both `trusted_shell` (v38.54) and `pgrp`/`fg` handoff (v38.53), `vmm.c` clone/free skips `PAGE_DEV`, `README` Testing/Project Structure/Arch diagram now list `ahci/usb/fbmap`, new `docs/drivers/ahci.md` + `fbmap.md`, `OS_VERSION` bumped to 38.57. Build + `usb_test` + `fbmap_test` green. |
| v38.56 | **xHCI/USB 3.0 host + BOT mass-storage — SuperSpeed sticks become drives 8+ on the existing sector API.** The kernel now speaks USB 3.0 natively: a from-scratch xHCI driver (`src/drivers/xhci.c:1181`) enumerates `PCI 0C/03/30` (`qemu-xhci`, 64-bit BAR0 in the identity-mapped `0xFE000000..0xFF000000` window) and carries bulk-storage devices all the way to the VFS. Bring-up: halt+reset (HCRST), DCBAA (`MaxSlots+1`), command ring (CRCR, 256 TRBs + Link TC), event ring + ERST (interrupter 0, **poll model** — `IMAN.IE` stays off, completions found by watching the cycle bit, exactly like AHCI; no MSI/INTx), `MaxSlotsEn`, then `RUN`. Enumeration per port: PORTSC reset (preserve PP) → **Enable Slot** (slot id from completion) → **Address Device** (input context A0/A1, slot route=root port, EP0 `512` SS / `64` HS) → `GET_DESCRIPTOR` (Setup/Data/Status TRBs, IDT) → MSC BOT interface (`08/06/50`) → bulk EPs (`DCI=2*ep+dir`, type 2/6, `1024` SS / `512` HS) → `SET_CONFIGURATION` → **Configure Endpoint** (A0 + both bulk DCIs, `Context Entries = max DCI`). Data path: `CBW(31)` on bulk-OUT → data `Normal` TRB (DIR) → `CSW(13)` on bulk-IN, each `IOC` and awaited via `Transfer Event`, CSW sig/tag/status validated; `TEST UNIT READY` (retry + `REQUEST SENSE`), `INQUIRY` (36, type 0), `READ CAPACITY(10)` (512B), `READ10`/`WRITE10` (≤128 sectors via 64K-aligned bounce + `ata_batch_limit`). Integration: `kernel.c` `xhci_init` after `ahci_init`, `ata.c` routes `drive >= USB_DRIVE_BASE (8)` to `usb_read/write_sectors`, `vfs_mount.c` allows `8..11`, `run.sh` creates `usb.img` FAT32 + QEMU `-device qemu-xhci -device usb-storage,bus=xhci0.0` (SuperSpeed, `PORTSC speed 4`). Two bring-up bugs fixed: `xhci_cap8[8]` clobbered `evt_dequeue/cycle` with `"QEMU    "` on INQUIRY (36B into 8B) → `64B`, and `bmCBWFlags` was always `0x80` (IN) so `WRITE10` stalled → `is_in` param. CI: `scripts/usb_test.py` boots `qemu-xhci` + FAT32 stick, asserts `USB3 -> drive 8` + `[XHCI] ready`, `mount /usb fat32 8` (BPB via xHCI), `cp` inside volume + host `mtools` byte-compare. `OS_VERSION` bumped to 38.56. |
| v38.55 | **SMP VM-consistency: atomic COW resolution, stale-TLB guard, locked fork marking + waitpid reaping, dead-task waiter cleanup.** The kernel grew 4-core SMP on a single-lock design without cross-CPU VM primitives, and the review found the serious defects clustered where shared page tables meet concurrency — all fixed now. (1) **The COW fault handler holds `vmm_lock` across the entire check-through-PTE-update**: the old code took the lock only for the sole-owner *check*, so two cores faulting the same shared page interleaved between check and rewrite — both duplicated, the second PTE store clobbered the first thread's fresh mapping (silent data loss) and leaked a frame. The destination frame is allocated *before* the lock (frame_alloc takes vmm_lock itself), the PTE is re-read under it so a peer that resolved the fault first is detected (flush + resume instead of double-copy), and the old frame is freed after unlock. (2) **Stale-TLB guard in the #PF path**: a Ring 3 WRITE fault whose PTE is already PRESENT\|RW\|USER means a peer core promoted/duplicated the page and only this core's TLB is stale — it is resolved with one `invlpg` + resume; before, it fell through into `demand_map_zero`, which **replaced a live page with a zeroed frame** (sibling thread's data destroyed). (3) **fork's COW-marking loops take `vmm_lock`** — they mutate exactly the state (source RO+COW stores, refcount bumps) the fault handler mutates on other cores. (4) **waitpid reaps under `task_lock` with revalidation** — two sibling threads could double-reap one zombie (double FREE, `num_tasks` underflow, TLS cleared on a recycled slot); losing the race now restarts the scan. (5) **Dead tasks leave sem/futex waiter lists**: `wake_one` skips ZOMBIE/FREE entries (a wake spent on a corpse used to vanish), and teardown defers the list scrub via a pending bitmap drained by the BSP main loop — the eager version deadlocked `thread_test` because signal-driven teardown runs with `task_lock` held while sync paths order `sync_lock > task_lock` (classic AB-BA, both cores spinning IF=0). A full TLB-shootdown IPI remains documented follow-up: `schedule()` reloads CR3 unconditionally every switch, bounding stale-writable-TLB exposure to ≤2 ticks. Verified: fork/cond/thread/jobcontrol/mmapfile/app_smoke all pass under TCG *and* KVM. `OS_VERSION` bumped to 38.55. |
| v38.54 | **Four critical security/data-integrity fixes from the full-project audit.** (1) **fd `write()` no longer destroys files >4KB** — the descriptor write path spliced into a fixed `char whole[4096]` and rewrote the file from it, so any write past 4095 bytes silently truncated the file on ALL backends (a 1-byte append to a 100KB ext2 file erased 96KB); writes now read-modify-write through a right-sized heap buffer (`VFS_FD_MAX_FILE` = 8MB cap, POSIX short-write semantics), and `lseek()+read()` on `/ext2`/`/fat32` finally honours the offset via new range readers (`ext2_read_file_range` block-map walk with sparse holes as zeros, `fat32_read_file_range` cluster-chain skip). (2) **Shell-host privilege gates were forgeable** — SYS_EXEC_CMD/SYS_KILL_TASK/SYS_KILL compared the caller's launch_arg STRING against terminal/explorer/taskmgr, but any app can exec "taskmgr.mct" with arg "taskmgr.mct" (loader stores argv verbatim) and walk through into builtins that bypass VFS permission checks (delete/mount as non-root) and kill uid-0 tasks; trust is now the kernel-computed `trusted_shell` flag (resolved image identity + parent trust, recomputed on exec, inherited on fork), plus cross-uid/kernel-target kill denial for non-root. (3) **Deleting /etc/passwd re-armed the hardcoded default password** — root dirs are deliberately 0777 and delete authorization rides the parent's write bit, so unlinking the file made login fall back to "mectov123"; delete/rename gained uid-aware variants that refuse the protected path for non-root, and `passwd_ensure_initialized()` writes the default as a real salted-SHA-256 hash at first login so the plaintext fallback is unreachable. (4) **GDB stub stack overflow + Ring-3 debugger entry** — `m addr,400` wrote 2048 hex chars into `reply[512]` (~1.5KB of kernel stack smashed inside the trap ISR, no auth on COM2); count is bounded to the buffer, int1/int3 drop DPL 3→0 (any Ring 3 program could `int3` into the debugger loop), and a `nogdb` boot flag disables the stub entirely. Regression battery green: app_smoke under TCG *and* KVM, lseekfile, fat32 (LFN verified with mtools), hardening. |
| v38.53 | **KVM boot + app-launch fix — PAE PDPTEs may carry only the P bit.** Under `-enable-kvm` the OS reset instantly at paging enable, and after boot, launching any app panicked with #GP inside `vmm_switch_page_dir`; TCG was unaffected, which is why every CI harness passed. Root cause: this host's KVM validates PDPTE reserved bits when loading them (CR0.PG 0→1 and every CR3 reload) and treats R/W and U/S — architecturally "ignored" per SDM Table 4-7 — as reserved, raising #GP(0) for any present PDPTE carrying them. Every PDPTE write now carries ONLY PAGE_PRESENT: `paging_init`'s three boot-table sites, `space_pd()`, the USER widening in `space_pt()` (PD-level widening kept), and fork's parent-flag propagation. Permissions are unaffected — the CPU enforces R/W/U/S from the PD/PTE levels down. Diagnosed by bracketing `paging_init` with serial prints (fault pinned to the exact `mov %eax,%cr0`) then bisecting the PDPTE flags; verified end-to-end under KVM `-cpu host -smp 4` including the icon-double-click terminal launch that previously froze. |
| v38.53 | **Ring 3 scanout takeover — the display-server foundation.** A Ring 3 task can now own the actual framebuffer: **`SYS_FB_MAP` (118)** maps the VBE linear framebuffer read/write into the caller's address space and returns the geometry (`fb_info_t {base,width,height,pitch,bpp}`, new type in `types.h` — the common base of the kernel `task.h` and the Ring 3 ABI). The mapping is DEVICE memory: every PTE carries the new **`PAGE_DEV`** software flag (available bit 11, beside `PAGE_COW`/`PAGE_SHARED`), so `vmm_clone_address_space` skips it entirely (a child never inherits the scanout) and both address-space free walkers skip it — which also closes a latent bug: the clone walker's refcount bump was unbounded, so a fork after any MMIO mapping above the RAM ceiling would have written past `frame_ref_count[]` into kernel BSS. Pages map eagerly with RW+USER+NX (W^X: pixel data is never executable) into the caller's own page directory (kernel tasks with `page_dir==0` are refused — device pages must never enter the shared boot tables). Authorization is logind-style, not setuid-style: uid 0 or the controlling terminal's foreground process group (the active console session); background jobs are refused. While a live task holds the scanout, **the kernel main loop stops presenting frames** — no `full_redraw`, no cursor, no WM input processing; it keeps only non-pixel duties alive (keyboard pump to the holder, GDB stub, net poll, zombie reaping, auto-lock tick) so a takeover cannot starve them. Keystrokes route to the holder through the same queue `SYS_GET_KEY` pops (the gate now accepts the scanout owner alongside the terminal's foreground app); raw mouse state was already global via `SYS_GET_MOUSE`. **`SYS_FB_RELEASE` (119)** hands the pixels back (owner-only); exit/kill drop ownership through a `task_cleanup` hook and exec discards it with the region table, each restoring the desktop on the next tick. The spawn/handoff race closed along the way: pgrp + foreground handoff moved INSIDE the loader's interrupts-disabled window (`load_mct_app_fg`) — previously an app could run on another core and hit an authorization check before `cmd_run` finished handing over the terminal. `apps/fbmap.mct` animates a gradient + bouncing box straight to the scanout, drains queued keys, then chases the mouse until any key releases; `scripts/fbmap_test.py` (wired into CI) proves scenario A (grant → interactive takeover → typed key routed through the pump → clean release → `[FBMAP] desktop restored`) AND scenario B (relaunch, Ctrl+C mid-takeover → ownership drops via cleanup → desktop restored, zero `[PANIC]`). All 11 local regressions green. `OS_VERSION` bumped to 38.53. |
| v38.52 | **Kernel hardening: CSPRNG, unpredictable TCP ISNs, salted-SHA-256 passwords.** The kernel now carries a real entropy pool + ChaCha8 DRBG (`src/sys/entropy.c`): `entropy_init()` seeds at boot (TSC jitter + kernel-address mixing) and `entropy_add()` feeds it continuously from the PIT timer IRQ, every PS/2 keyboard byte and every mouse byte, re-keying the DRBG every 8 samples. `/dev/random` stopped being a fixed-seed LCG (`vfs.c` now returns `get_random_bytes()`), TCP ISNs for both the client (`net.c` connect path) and the SYN-ACK (listen path) come from the CSPRNG with a `get_ticks` fallback only for the pre-seed microseconds, and a new `SYS_GETRANDOM` (117) exposes the DRBG directly to Ring 3 (POSIX `getrandom(, , 0)` shape). Passwords are no longer stored in plaintext: `passwd` writes `/etc/passwd` as `<8-byte-salt-hex>:<sha256(salt_hex‖pw) hex>` (new `src/sys/sha256.c` + `src/sys/passwd.c` rewrite); login and the `passwd` command verify through `sys_verify_password()`, which still accepts a legacy plaintext file until the next password change. `scripts/hardening_test.py` (wired into CI) boots, runs `passwd mectov123 hunter2`, proves the NEW password unlocks the lock screen while the OLD one is rejected (a plaintext file would accept both), then `apps/hardening_test.mct` proves from Ring 3 that `SYS_GETRANDOM` returns fresh non-zero bytes, two `/dev/random` reads differ, and the passwd file is 81 chars of `salt:hash` with no plaintext inside. `OS_VERSION` bumped to 38.52. |
| v38.51 | **Idle auto-lock (`locktimeout <seconds>`)** — the kernel now tracks the last keyboard/mouse input (`security.c`: `last_input_tick`, bumped by the main loop's mouse and keyboard hooks) and, when armed, raises the same `pending_lock` flag as the `lock` builtin once input has been quiet for the timeout (checked at 1 s granularity next to the clock tick). The `locktimeout` shell builtin sets it (0 disables, no-arg prints the current value, capped at 24 h); after unlocking, the countdown is re-anchored so the desktop does not immediately re-lock. `scripts/autolock_test.py` (wired into CI) arms 20 s, goes quiet and watches the login gate appear on its own, unlocks (session preserved, no re-lock), then disables and confirms a longer quiet window passes with no lock. `OS_VERSION` bumped to 38.51. |
| v38.50 | **AHCI/SATA driver — SATA disks become drives 4+ on the existing sector API.** The controller is found on PCI (class 01/06/01), BAR5 mapped inside a widened identity PCI MMIO window (`mem.c`: the two APIC-only PTs became 8 strongly-uncached PTs covering `0xFE000000..0xFF000000`); ports with a plugged ATA disk register as drives `4..7`, so ext2/FAT32 and the runtime `mount()` work on SATA volumes with zero changes to the filesystem layers. Transfers use command slot 0 + a single PRD into a 64K-aligned bounce buffer, LBA48 READ/WRITE DMA EXT (`0x25`/`0x35`), completion by polling PxCI under `ahci_lock` with IRQs off (mirrors the IDE DMA model). Two real bugs found while bringing it up: the H2D FIS fields were written one byte off the spec layout (count landed in the LBA slot, device in LBA 31:24, and the real count field stayed 0 → the device read a 64K-sector window at LBA 0x40000001 and returned zeros), and `vfs_is_file()` never accepted `FS_FAT32_FILE`, so every file op on a mounted FAT32 volume reported "source not found" (v38.42 mount gap). Also fixed: port slots now remember their physical port (non-contiguous port maps broke reads), the BAR page keeps the window's PCD|PWT uncached flags, and ATAPI (optical) signatures are skipped. CI: `scripts/ahci_test.py` boots with `ich9-ahci` + a FAT32 SATA disk, mounts it (BPB read through AHCI), copies a file inside the volume (read + write), and byte-compares it on the host with mtools. `OS_VERSION` bumped to 38.50. |
| v38.49 | **PAE 3-level paging + EFER.NXE with W^X — user heap, stack and anonymous mmap pages are non-executable, and executing data kills the task.** The whole paging layer moved from 2-level (1024-entry, 32-bit PTE) to PAE (PDPT 4×512MB → PD 512×2MB → PT 512×4KB, 64-bit `pte_t` entries): `paging_init` builds static `boot_pdpt`/`boot_pds` + 256×2MB identity PTs and sets CR4.PAE before CR0.PG; the SMP trampoline sets CR4.PAE per-AP and every AP repeats the EFER.NXE MSR write (first `rdmsr`/`wrmsr` helpers in the tree, CPUID-gated with a clean "NX unavailable" fallback). All page-table walkers migrated (COW handler, heap/stack demand fill, guard pages, mmap fault/munmap, `user_page_ok`, `SYS_VMM_FREE`, SysV shm, `vmm_create/clone/free/map/unmap`), and `vmm_map_page` takes `uint64_t` flags so `PAGE_NX` survives. W^X policy: demand-zero heap/stack pages and anonymous mmap pages map with PAGE_NX; images and the signal trampoline stay executable. A page fault with the I/D bit (instruction fetch) is **never demand-mapped** — it logs `[W^X] execute fault` and delivers SIGSEGV (Ring 3) or panics (Ring 0); before this, a fetch into unmapped heap got a zero page mapped and *executed*. `apps/nxtest.mct` places a `ret` on its stack and calls it — CI (`scripts/nxtest.py`) asserts the kill. Migration also uncovered and fixed a real fork leak: the old clone re-allocated a PT for every kernel-region PDE and overwrote the copy `vmm_create_address_space` had just made (~120 frames ≈ 0.5 MB leaked per fork); the PAE clone COW-copies only the USER ptes inside kernel tables and reuses the create-time copies. All QEMU harnesses now pass `-cpu qemu32,+nx` (the default qemu32 model lacks the NX CPUID bit). 11/11 local regressions pass on the PAE kernel (boot/fork/doom/panic/fat32/thread/mmapfile/mount/poweroff/fputest/socktest) + nxtest. `OS_VERSION` bumped to 38.49. |
| v38.48 | **O(1) used-frame accounting + dead code removal.** `phys_get_used_pages()` used to scan all 131,072 `frame_ref_count[]` entries under `vmm_lock` with IRQs off every time `/proc/meminfo` or `get_sysinfo` was read; it is now a maintained counter (`phys_used_count`, updated at the five bitmap-mutation sites — `phys_init`, `phys_reserve_region`, `frame_alloc`, `frame_free`; direct refcount bumps from COW/shm/fork correctly leave it alone since they never move a frame between free and used) read lock-free. Verified live: `MemUsed 48244 kB` on a fresh boot = 48 MB kernel reservation + pinned zero page + runtime allocations. Also deleted `vmm_find_free_region()` — a never-rewinding bump allocator that looked like a VA-space leak but turned out to have **zero callers** (real mmap placement already uses the first-fit gap search in `mmap_region_alloc`). `OS_VERSION` bumped to 38.48. |
| v38.47 | **ATA DMA bounce buffer preallocated + memcpy.** The bus-mastering DMA path kmalloc'd a bounce buffer per I/O and copied up to 64 KB **byte-by-byte** in both directions; it is now a static 64K-aligned 64 KB buffer with plain `memcpy` (both copies stay under `ata_lock` since the buffer is shared across CPUs). Being 64K-aligned also guarantees a ≤64K transfer never crosses a 64K PRDT boundary, so the descriptor table is always a single region. `bigread_test` asserts DMA engaged with zero failures; FAT32 write regression green. `OS_VERSION` bumped to 38.47. |
| v38.46 | **Flat desktop icon restyle — saturated flat squircles, no shadows.** The icon tiles dropped the old muted fills for a modern flat palette (near-black Terminal, blue Explorer, amber Browser, sky PCI, green Snake, violet Calc, fuchsia ELF…), the Terminal prompt is neon green, and Notepad finally gets a real page glyph with rule lines (its old white-on-light-gray glyph was nearly invisible). Labels stay plain white text with a 1px shadow. Geometry untouched — grid, hit boxes, drag/double-click and `icons.cfg` all identical, so every CI harness that double-clicks the Terminal icon at (60,64) keeps working. `OS_VERSION` bumped to 38.46. |
| v38.45 | **ACPI S5 poweroff from the firmware's own tables.** `acpi_init` now records the **FADT** in addition to the MADT (both the XSDT and legacy RSDT paths scan every entry — the old early-return after the MADT could miss the FADT depending on table order), parses the PM1a/b control-block addresses, and walks the DSDT AML for the `\_S5` package to get the real SLP_TYP values. `shutdown()` writes PM1a/PM1b with SLP_EN (bit 13) + SLP_TYP (bits 12:10) before falling back to the legacy hardcoded ports — under QEMU the VM now powers off through the spec path. `scripts/poweroff_test.py` (wired into CI) types `shutdown` and asserts the **QEMU process exits by itself**. `OS_VERSION` bumped to 38.45. |
| v38.44 | **Capacity bumps: TCP connections 8→16, global fd table 128→256, windows 16→32.** Socket-per-connection servers and window-heavy sessions were the pressure points. `MAX_TASKS` (GDT TLS coupling) and `MAX_NODES` (on-disk MECTOVFS layout) deliberately unchanged, documented in code. `OS_VERSION` bumped to 38.44. |
| v38.43 | **POSIX socket API, fd-integrated — `socket`/`bind`/`listen`/`accept`/`connect`/`sendto`/`recvfrom` (syscalls 108–114).** `socket()` and `accept()` return ordinary file descriptors, so `read`/`write`/`close`/`poll`/`select` are socket-aware. The listener model is now POSIX-shaped: a LISTEN slot **never mutates into a connection** — each inbound SYN spawns a fresh child slot (`listen_parent` link, free slots = the backlog) that completes the handshake on its own, and `accept()` hands out ESTABLISHED children one per call (POLLIN on a listener means "accept would return"). `connect()` returns once the SYN is out; POLLOUT means established. UDP datagram sockets use the stack's single global binding with `net_udp_peer()` feeding `recvfrom` source addresses. `apps/tcpserver.mct` exercises both directions (client echo against a host server at 10.0.2.2:9999, server accept through QEMU hostfwd with PING→PONG); `scripts/socktest.py` (wired into CI) drives it live with a Python echo server on the host. `OS_VERSION` bumped to 38.43. |
| v38.42 | **Runtime mount table — `SYS_MOUNT` (115) / `SYS_UMOUNT` (116) + `mount`/`umount` shell builtins.** A mount point is an empty VFS directory node retyped to the backend's `FS_*_DIR` (the existing type dispatch then routes I/O to the real filesystem); the new `src/sys/vfs_mount.c` records which drive + root key backs each mount so umount can undo it. The boot mounts (`/ext2` drive 1, `/fat32` drive 3) register through the same table in both `vfs_init` paths. `umount` drops the subtree (disk data untouched), restores a plain directory and persists the node table; `mount` with no args lists active mounts. Root-only at the syscall layer; documented limitation: the ext2/FAT32 backends keep one global superblock each, so a runtime mount targets the boot drives (or a remount). `scripts/mount_test.py` (wired into CI) does the full roundtrip: umount `/fat32` → `umount /apps` refused → remount → fat32demo passes on the remounted filesystem. `OS_VERSION` bumped to 38.42. |
| v38.41 | **Eager FPU/SSE context switching — DOOM and Ring 3 float code can finally be preempted against each other.** Until now the scheduler never touched the FPU: the kernel core is soft-float and DOOM was treated as the system's ONLY x87 user (it runs inside the shell's task in kernel mode), so its state survived preemption purely because nothing else emitted FPU instructions — any second FPU user would silently corrupt it. Every `task_t` now carries a 16-byte-aligned 512-byte `fxsave` image (`src/sys/fpu.c`), and `schedule()` swaps it eagerly (fxsave out / fxrstor in) with IF=0 under `task_lock` — SMP-safe with no per-CPU owner tracking. `fpu_init_cpu()` (BSP in `kernel_main`, every AP in `ap_main` — CR4 is per-CPU) sets CR0 EM=0/MP=1/TS=0 and CR4 OSFXSR|OSXMMEXCPT behind a CPUID check with a no-op fallback, and builds the canonical `fninit` clean-state template. Lifecycle: task creation seeds the clean image, `fork()` inherits the parent's live state (flushed first — POSIX), `exec()` resets to clean. #NM is a logged safety net (the eager scheme never sets TS); #XM from Ring 3 delivers the new `SIGFPE` (8). `apps/fputest.mct` proves the exact scenario the old design could not survive: two forked processes accumulating in the live x87 register stack AND an SSE register across preemptions and across fork — both sums exact; `scripts/fputest.py` (wired into CI, also under KVM) asserts it. `OS_VERSION` bumped to 38.41. |
| v38.40 | **Start menu search-as-you-type — open the menu and just type to filter the 12 items.** While `start_menu_open` is set, the kernel routes printable keys, Backspace, Escape and Enter to the taskbar instead of the focused window. `taskbar.c` keeps a small query buffer and a case-insensitive substring filter over the menu labels (hoisted into a shared `sm_labels[]` so draw, hover and click all key off the ORIGINAL index through the filtered list): the query renders live in amber in the menu header ("Search: …"), only matching items are drawn (starting at the same first row, no gaps), "No results" appears for a miss, Backspace pops the query, Escape clears the query then closes the menu, and Enter launches the hovered (or first) filtered item. The filter is a real substring match — typing "lock" shows **Clock and Lock** ("Clock" contains "lock"). Hover and click hit-tests use the same filtered list, so a filtered menu is fully clickable. New `scripts/startmenu_search_test.py` (wired into CI) boots once and drives it all live: "Snake" narrows to one row and Enter launches the game (loader log grows), "zzz" shows "No results", backspace x3 restores all items, "lock" narrows to the two matching rows, and Escape clears then closes. Also fixes a hover glitch exposed by the menu work: `taskbar_draw` early-returned when the dirty rect stayed **above** the taskbar strip, so moving the mouse inside the Start menu never repainted the hover highlight — it stuck on the previous row. When a popup (Start menu/calendar/volume) is open, the early-return now also accounts for the popup's bounds, so the highlight follows the mouse (new "in-menu move" check in `scripts/taskbar_hover_test.py`). `OS_VERSION` bumped to 38.40. |
| v38.39 | **Taskbar window buttons feel complete — hover feedback is wired up (it was dead code), and minimize/restore from the taskbar is verified end to end.** Clicking a focused window's taskbar button minimizes it and clicking again restores it (the click handler already existed; it is now covered by CI). The hover system was never actually running: `taskbar_track_mouse` was declared and defined but **never called** from the kernel loop (unlike `wm_track_mouse`), and even had it been, hover changes never requested a redraw — pure mouse moves only blit the back buffer, so highlights appeared only when some unrelated event (e.g. the 1 s clock tick) happened to trigger a full redraw. The kernel now calls `taskbar_track_mouse` on every move, and the function computes the desired hover state from scratch and sets `needs_redraw` only when it changed. Hovering a **non-focused** window button turns its title amber (`TB_ACTIVE`, echoing the Start-menu selection accent), the focused button stays plain, the START label highlights on hover, and the Start menu's amber selection bar now actually appears over items (the same dead system). New `scripts/taskbar_hover_test.py` (wired into CI) drives it live: minimize/restore a terminal via its button (pixel-compared region), hover the non-focused button with two windows open, verify the focused one stays plain, verify the highlight clears on move-away, and check the Start menu item bar. `OS_VERSION` bumped to 38.39. |
| v38.38 | **waitpid lost-wakeup fix — a parent could sleep forever even after its child became a zombie.** `task_waitpid` scanned for a zombie and then parked (set `waiting`, `TASK_STATE_BLOCKED`, `rq_remove`) without re-checking under the lock, while `terminate_task` only wakes the parent if `waiting != 0` — so a child exiting in the window between the scan and the park was turned into a reaped-able zombie with nobody woken, and the parent `hlt`-parked permanently (no timeout, unlike `task_sleep`). This latent race (in place since v36.0) intermittently hung `rlimittest`'s `fork`+`waitpid` — surfacing as flaky CI failures on `scripts/rlimit_test.py` and `scripts/ulimit_test.py` ("rlimittest did not pass after ulimit changes", `[RLIM] start` logged but no `ALL PASS`). The park now re-checks for a zombie **under `task_lock`** (the same lock the exit path holds, so the check cannot race it): if one appeared, the park is undone (state back to READY, still current on its core — the scheduler's keep-current path re-enqueues it if a tick preempts first) and the loop reaps it on the next pass. Verified: `ulimit_test` 4/4, `rlimit_test` 3/3 (both previously failing intermittently), plus `fork`/`jobcontrol`/`thread`/`app_smoke` regressions all pass. `OS_VERSION` bumped to 38.38. |
| v38.37 | **Full-black theme — the warm/orange tint is gone from the taskbar AND the window chrome.** The Instrument-Console taskbar used a warm charcoal face (`TB_BG`/`RETRO_FACE` 0x16130F) with an amber-brown bevel highlight (0x3C2E18) and an amber logo square on the Start button, and focused windows carried an amber hairline border with amber control glyphs — together they read as "orange/yellow" across the desktop. Taskbar surfaces are now **pure black** (bevels neutralized to subtle grays; these constants are only consumed by taskbar bevels) and the Start button is a centered off-white **"START"** label with no logo. Window chrome (ToaruOS-style) is black too: `TOARU_TITLE`/`TOARU_BORDER` pure black (no more yellow line around the focused window), title text and the close/min/max glyphs off-white. Amber survives only as selection feedback in the Start menu/calendar popups. `scripts/lock_test.py`'s taskbar detector now looks for near-black instead of the old 0x16130F charcoal. Verified pixel-wise: all four window border edges black, **zero amber pixels** in the chrome envelope, off-white title + buttons legible, Start menu black with all 12 items. `OS_VERSION` bumped to 38.37. |
| v38.36 | **DOOM windowed fix — the game was opening an empty window because the login gate hogged the kernel heap.** After the v38.30 redesign, `gui_login()` cached three framebuffer-sized buffers (scaled wallpaper, blurred wallpaper, transition snapshot — ~12 MB) and never released them, so a later `doom` launch failed in `Z_Init` with "Unable to allocate 5 MiB of RAM for zone" — the WM window opened but the game loop exited before rendering a frame. A new `free_login_buffers()` (login.c) returns all three to the 24 MB kernel heap the moment the gate closes (both Enter and Sign-In paths); each rebuilds lazily on the next login. Verified: `[DOOM] Entering game loop...` + tick lines after a normal login, window content pixel-checked (game visible, not a blank window), and the lock/login suites still pass. `OS_VERSION` bumped to 38.36. |
| v38.35 | **Lock-screen date fix — the day/date/month/year under the clock is back.** `gui_login()` cleared `date_str` on entry (every boot, logout and lock), but `refresh_clock()` only rebuilt it when the RTC day changed — and its `last_day` static survives across calls, so a second visit to the login gate (lock, logout) left the string empty and the date invisible while the clock kept ticking. The rebuild now also triggers when `date_str` is empty (same guard added for the clock string), so the full date renders on every lock — verified pixel-wise (off-white glyphs below the amber clock, readable "Saturday, 15 August 2026"). `OS_VERSION` bumped to 38.35. |
| v38.34 | **Lock screen feature — lock the desktop without ending the session.** A **Lock** item in the Start menu (padlock glyph, 12-item menu), the **Ctrl+Alt+L** shortcut (consumed in the kernel so it never types an `l`), and the `lock` shell builtin all funnel into a new `pending_lock` flag (`security.c`; the old text-mode stub printed "System Locked. Please enter password." into the console and never verified anything — `is_locked` was set and never read, so it's replaced). The kernel main loop (kernel.c) notices the flag, runs the existing graphical login gate `gui_login()` **without** `wm_reset_session()` — every window, terminal and running task stays alive behind the lock — and on unlock `full_redraw()` restores the desktop. `scripts/lock_test.py` (wired into CI) boots, logs in, launches the Terminal, locks via the `lock` command, unlocks and pixel-compares the terminal window pre/post (session preserved, diff 0.1), then locks again via Ctrl+Alt+L. Start-menu geometry now comes from `START_MENU_H`/`START_MENU_ITEMS` in `taskbar.h` (shared by draw, hit-test and kernel popup-dismiss). `OS_VERSION` bumped to 38.34. |
| v38.33 | **The lock screen is now completely bare — wallpaper and clock only.** The "press SPACE to sign in" hint and the "v38.xx SMP MECTOVFS 1024x768" version footer are gone (`draw_footer` and the hint in `draw_lock_screen` removed, along with the now-unused `str_advance`), leaving the full-bleed wallpaper and the bottom-left clock + date as the only elements. SPACE (or a click) still opens the password entry and the 4-second idle fallback is unchanged. `scripts/login_redesign_test.py` now asserts the two bottom-center bands (hint y and footer y) carry zero dim-gray text pixels on the lock screen. `OS_VERSION` bumped to 38.33. |
| v38.32 | **Animated screen transitions — the lock and password screens cross-fade into each other.** Pressing SPACE no longer swaps the screen instantly: the current frame is snapshotted into a 4 MB `prev_frame` buffer (allocated lazily, `kmalloc`) and the destination screen (blurred wallpaper + panel, or sharp wallpaper + clock/hint/footer) is rendered underneath, then a fixed-point lerp pass (`blend_transition`, t in 0..256, no floats in the kernel) blends the two — so the open animation *is* the blur animation (sharp wallpaper eases into the 5-tap blur), the caption fade-out, and the panel fade-in all at once, ~0.45 s of guest time per direction (smooth under KVM, progressively slower but fluid under TCG). The idle 4-second revert now plays the same fade in reverse (blur eases out, clock/hint fade back in, panel fades away), and the gate state (input, error, shake) resets only when the fade completes. Input stays honest during the animation: keys typed while the panel is opening are accepted normally, and any key (or click) during the *closing* fade cancels it and brings the panel back with the password intact, restarting the 4 s clock. If the 4 MB snapshot allocation fails the switch falls back to the old instant behavior. `scripts/login_redesign_test.py` now waits for the fade to settle (blur bar) before the content checks and retries through the closing fade for the revert + clock-returned assertions. `OS_VERSION` bumped to 38.32. |
| v38.31 | **Password entry gets full focus — blurred wallpaper, no clock, no footer.** While the login panel is open, the screen now does everything to make you type the password and nothing else: the wallpaper is run through a cached **5-tap separable box blur** (two sliding-window passes, O(n) with row-major access, so the build is instant on real hardware and still fast under TCG) and the bottom-left clock/date and the version footer are **not drawn** on this screen — they only live on the lock screen now. The 4-second idle fallback is measured in **RTC (wall-clock) seconds** — PIT ticks drift against the wall clock under QEMU TCG (a raw 4000-tick window collapsed to ~1 real second there), and the anchor is rounded up to the next whole second so the window is a genuine ≥4 s on KVM, TCG and hardware alike. The countdown only starts once the panel has actually rendered: building the blur must not count as "idle", which previously made the panel flash away before it ever showed. `scripts/login_redesign_test.py` now also asserts the panel *stays* on screen, the clock is gone, the footer is gone, and the wallpaper sharpness (mean horizontal neighbor-difference) drops by at least 45% on the password screen. The lock screen is now branding-free — the old "MECTOV OS" wordmark is gone, leaving wallpaper + clock + hint. `OS_VERSION` bumped to 38.31. |
| v38.30 | **Redesigned login screen — full-bleed wallpaper, bottom-left clock + full date, SPACE-only password entry with a 4-second idle fallback.** The graphical gate now shows the wallpaper **scaled (nearest-neighbor) to fill the entire framebuffer** — never cropped, never covered by the old charcoal dim layers (a single cached scaled copy is built per resolution and memcpy'd every frame). The old centered hero clock is gone: a large **digital HH:MM:SS clock with blinking colons plus the full date — day name, day, month and year — is pinned to the bottom-left corner** of the lock screen (`draw_clock_corner()`; v38.31 moved the password screen to a blurred, caption-free focus view). Opening the password entry is now deliberate: **only SPACE (or a click) dismisses the lock screen** — any other key is ignored so the gate stays clean — and the hint reads "press SPACE to sign in". Once the password panel is open, **if no key is typed for 4 seconds the screen automatically falls back to the lock screen** (input, error and shake state reset), so the box never sits parked on an empty password prompt. Full date now includes the year (e.g. `Monday, 15 August 2026`), drawn from `rtc_time_t.year`. New `scripts/login_redesign_test.py` (wired into CI) boots, asserts the wallpaper is full-bleed + amber clock glyphs exist at bottom-left, verifies a non-SPACE key does NOT open the panel, SPACE does, 4 s of idle reverts, and the password still logs in. `OS_VERSION` bumped to 38.30. |
| v38.29 | **Windowed DOOM — the kernel-embedded game now renders into a WM window like any other desktop app** instead of grabbing the whole framebuffer. `doom_start` opens a centered 640×400 kernel-owned window (2× the internal 320×200 screen — `DOOMGENERIC_RESX/RESY` cut from 1024×768 to 640×400) and hands every rendered frame to the compositor: `DG_DrawFrame` marks the window dirty instead of blitting `fb_addr`, and a new `doom_win_draw()` callback (wm.c `draw_one`) integer-scales `DG_ScreenBuffer` into the window's content buffer with bounds clipping, so the window is freely draggable/resizable/maximizable and stays centered at any size. The desktop never stops rendering: the FPS HUD, cursor, taskbar and other windows all keep working, and clicking another window moves focus (DOOM keeps animating in the background). Input follows the WM: while the DOOM window holds focus, the main loop forwards every raw scancode — press AND release, which the normal character path drops — straight to DOOM's key queue via `doom_handle_scancode()` (typematic-filtered, gui_lock-guarded against the SMP producer/consumer split); legacy `doom -fullscreen` still polls the keyboard directly. The serial `[DOOM] tick` heartbeat + on-screen frame counter stay, now drawn into the window's own buffer. Closing the window (X button) sets `doom_running = 0` via the `wm_is_open` check and returns to the desktop cleanly. `src/include/types.h` fixes `int8_t` to `signed char` so doom TUs can include `wm.h` (plain `char` is a distinct type per the standard; identical on i386). New `scripts/doom_test.py` (wired into CI) boots, launches the Terminal, runs `doom`, and asserts a WM window opens, the game loop enters, and `[DOOM] tick` lines prove frames are being composited without crashing the OS. `OS_VERSION` bumped to 38.29. |
| v38.28 | **POSIX resource limits (RLIMIT_NPROC / RLIMIT_AS / RLIMIT_NOFILE) with fork-bomb and fd/memory-leak protection.** Every task now carries a soft/hard limit triple (`rlimit_t {cur,max}` per resource, defaults NPROC 64, AS 256 MB, NOFILE 16 — inherited by fork/exec/thread-create like Linux). **`SYS_GETRLIMIT` (106) / `SYS_SETRLIMIT` (107)** expose them from Ring 3 with POSIX privilege rules: a non-root caller may lower `cur` and raise it only up to `max`, but may never raise `max`; root may set anything; absurd values are rejected (EINVAL when `cur > max`) so no enforcement arithmetic can overflow. Enforcement sits in the hot paths: **RLIMIT_NPROC** is checked at `fork_common`/`thread_create_ex`/`task_fork_exec` before a single byte of address space is cloned (it counts live tasks sharing the caller's uid, exactly like Linux — not just direct children, so a fork bomb can't hide behind re-parenting); **RLIMIT_NOFILE** is checked at `task_map_fd` (open/pipe) and at `dup2` onto an empty slot; **RLIMIT_AS** is checked at `SYS_MALLOC` heap growth and both `task_mmap_reserve`/`task_mmap_file` before the region slot is consumed, against the committed total (heap break + all reserved mmap regions). Root (uid 0) bypasses everything, so the desktop, terminal and shell are never at risk. The shell gained a bash-style **`ulimit`** builtin (`ulimit -a` lists all three resources with soft/hard values, `ulimit -n 8` / `-u 4` / `-v <bytes>` lower the soft limit, `-H` targets the hard limit, and a value query prints the current soft limit) — `scripts/ulimit_test.py` drives it end-to-end from the terminal, proving inheritance by showing rlimittest still passes after limits were lowered and restored. `apps/rlimittest.mct` proves the syscall model from Ring 3 (defaults sane; fork refused under NPROC cur=1 then allowed after raising cur; hard-limit raise refused for non-root; NOFILE stops at cur=4 and closing a descriptor frees a slot; mmap 2 MB refused under a 1 MB AS limit, 256 KB allowed, then re-allowed after raising AS) and `scripts/rlimit_test.py` runs it end-to-end in CI. `OS_VERSION` bumped to 38.28. |
| v38.27 | **Futex mutexes + condition variables, and a scheduler fix for self-blocking wakeups.** `apps/lib/sync.h` is a pthread-style runtime on top of `SYS_FUTEX_WAIT`/`SYS_FUTEX_WAKE` (65/66): `mct_mutex_t` takes the fast path with one `lock cmpxchgl` and parks only when contended; `mct_cond_t` does wait/signal/broadcast with a sequence counter so a signal that races the sleep is never lost (the kernel's `futex_wait` re-checks the value under `sync_lock` and returns -1, and callers use the standard `while (pred) wait()` loop). `apps/conddemo.mct` proves them on 4 cores: 4 threads bump a shared counter to exactly 4×LOCK_ITERS=20,000 under the mutex (a broken lock loses updates), then 2 producers + 2 consumers move 3,000 items through an 8-slot bounded buffer over `not_full`/`not_empty` with no loss, no duplicates, no corruption; `scripts/cond_test.py` boots it in CI and asserts ALL PASS + zero `[PANIC]`. Debugging it to 8/8 clean runs uncovered a real SMP wake-path bug and two latent deadlock windows, all fixed: (1) `task_set_state` now **preserves IF** (irqsave/restore) instead of unconditionally re-enabling interrupts — the old `sti` opened a preemption window where the timer parked a task that still held `sync_lock`, and since waking it needs that same lock, the system deadlocked circularly; (2) a task that blocks itself mid-syscall (futex/sem/waitpid park) stays **current on its home core** until the timer preempts it — enqueueing it on a foreign runqueue let a peer core iret a stale saved frame (double-run), which is how an idle task's tid ended up parked in a futex waiter list; `task_set_state` now skips the enqueue while the task is still current anywhere, and `schedule()` re-enqueues it when it finally preempts; (3) idle tasks are structurally isolated — pinned to the core matching their tid, never pickable/enqueuable/wakeable elsewhere (`rq_enqueue`/`rq_pick`/`rq_steal` guards + `wake_one` drops a corrupted idle entry + `task_set_state` refuses the transition), and `schedule()` no longer clears `current_task[cid]` when nothing else is runnable (that lost the identity of a self-blocked task still executing its park loop). Safety net: the IRQ stub now sanity-checks the frame ESP it is about to iret — it must lie inside the current task's kernel stack, so a corrupted saved frame is a logged panic with a 4-core dump instead of a silent triple-fault. `OS_VERSION` bumped to 38.27. |
| v38.26 | **Bus-mastering IDE DMA — the CPU leaves the disk data path entirely.** The QEMU PIIX3 exposes a BMIDE register block (PCI BAR4, bus-master capable prog-if). `ata_dma_init()` finds the IDE controller after `pci_scan()`, maps BAR4 (I/O), enables PCI bus-mastering (command bit 2) and keeps PIO as the automatic fallback when no controller exists. A transfer builds a **PRDT** (8-byte entries: physical address + 16-bit byte count, `0` = 64 KB, bit 31 = EOT; entries are split at 64 KB boundaries) in a 64 KB-aligned static table, programs the command block with the READ/WRITE DMA opcode (0xC8/0xCA) and sector count, writes the PRDT address to BMIDE +4, then starts the engine (BMIDE command bit 0 — QEMU's PIIX starts on *bit 0* and only reacts to an edge, so the driver writes stop-then-start). Because the kernel heap is identity-mapped, the kmalloc bounce buffer's address *is* its physical address — PRD entries point straight at it, and the caller's user buffer is filled by a memcpy afterwards (or pre-filled for writes, which then CACHE FLUSH). IRQ14/15 are fully wired (IDT gates + PIC unmask + IOAPIC routing + `irq14`/`irq15` asm stubs) and acknowledge/clear the latched controller interrupt; the driver itself polls the BMIDE status (active bit or the latched INT bit = done — bit 2 is the *completion* flag, not an error, and error bits are 1/5/6). Three bring-up bugs fixed: (1) the start bit was written to bit 1 and direction to bit 0 (real-spec positions), but QEMU's PIIX starts DMA on bit 0 — reads never started and "completed" with zeros while the accidental `0x01` on writes happened to be the start bit, so writes worked; (2) QEMU can complete the entire transfer inside one VM exit, so the active bit can be set and cleared between two guest reads — the latched INT bit must be accepted as completion (and any stale INT/ERR latch cleared before starting), otherwise fast transfers false-fail; (3) direction comes from the ATA opcode, not the BMIDE register. Measured on `/bench.big` (160 KB, >128 KB page-cache ceiling): KVM **~42 ms → ~1.33 ms (~31×)** vs the v38.25 batch-PIO path, and ~510× vs the original per-sector word-loop (~680 ms); TCG ~13.4 ms (rep-insw unchanged there — no VM exits to save). `scripts/bigread_test.py` now asserts `[ATA] BMIDE DMA ready` in the serial log and zero failed DMA transfers in CI, on top of the existing byte-pattern + zero-`[PANIC]` checks. `OS_VERSION` bumped to 38.26. |
| v38.25 | **Multi-sector PIO + `rep insw`/`rep outsw` — 16× faster large uncached disk reads.** The ATA driver read every 512-byte sector with its own command (BSY/DRQ polls + 256 word-by-word `inw`), so a file too big for the page cache (>128 KB) paid command latency and, under KVM, 256 VM exits *per sector* just to move data. New `ata_read_sectors_drive`/`ata_write_sectors_drive` issue ONE command per contiguous run of up to 16 sectors (clamped to the 128-sector LBA boundary — `ata_batch_limit()` is shared by the driver and every caller so advancement can never skip sectors), and each sector's data phase is a single `cld; rep insw`/`rep outsw` instead of a 256-iteration port loop. Wired into every disk path: both `vfs_read_file` loops (whole-file and offset-aware), the page-cache prime, the batched `vfs_write_file` (zero-padded tail), and the ext2 + FAT32 block/run helpers. Two bugs found during bring-up: a batched read wrote a full 512-byte sector straight into a caller buffer that might be smaller (the 4-byte magic check smashed its stack → garbage-EIP panic) — whole sectors now go into the caller's buffer but the partial tail sector is read into a temp and copied — and the driver's internal 128-boundary clamp silently shortened a transfer while the caller advanced by the original count (skipped sectors) — the clamp is now a shared helper every caller advances by. Measured on a 160 KB `/bench.big` (>128 KB, never cached; seeded at boot with a byte pattern `bigread` verifies on every read): under KVM ~680 ms → **~42 ms (~16×)** per read, with batching adding ~1.7× on top of `rep insw` (per-sector+loop 680 ms → rep-insw-only 71 ms → batched 42 ms); TCG gains ~11% (97.7 ms → 87 ms). `scripts/bigread_test.py` runs it in CI (pattern + zero `[PANIC]`; `--kvm` for real timing). `OS_VERSION` bumped to 38.25. |
| v38.24 | **Real per-thread TLS + `clone()` — pthread-style threads.** `%gs` was a flat user-data segment for every task, so there was no such thing as a thread-local variable. Now every task slot owns a private FS/GS GDT descriptor (DPL=3, base = the thread's control block), and the context-switch frame — `registers_t` + all three entry stubs (`irq_common_stub`, `isr_common_stub`, `isr128`) — saves and restores FS/GS like any other register, so a thread's `%gs:0` thread pointer survives preemption, syscalls, signals, fork and migration between CPUs. The per-CPU GDT grows from 8 to 136 entries (all CPUs keep identical TLS slots at the same selector numbers); `gdt_tls_update()/gdt_tls_clear()` repoint/wipe a task's descriptors when it gains/loses TLS or its slot is freed. New **`SYS_CLONE` (104)** — `EBX=entry, ECX=child_stack, EDX=tls_base` — creates a thread sharing the caller's address space and starts it with `%gs` already pointing at its TCB (parent gets the TID, child sees 0); **`SYS_TLS_SET` (105)** installs/removes the current thread's TLS by repointing the descriptors and rewriting the pending return frame. `apps/lib/thread.h` is a tiny pthread-style runtime (`mct_tls_init`, `mct_thread_create`, `mct_thread_join` via waitpid, `mct_tls_get` reading `%gs:0`). `apps/threaddemo.mct` proves isolation: the main thread and 4 cloned workers each keep a private counter inside their own TCB and bump it 20,000× — with working TLS every counter lands on exactly ITERS; a shared TLS would race/corrupt the values. `scripts/thread_test.py` boots it in CI (4 cores) and asserts ALL PASS + zero `[PANIC]`. Two subtle bugs found during bring-up: an AP-idle and a fork-exec frame constructor still used the old 3-register layout (iret'd garbage → #GP), and the stub epilogues initially popped DS before GS (invisible until a task had a non-0x23 selector — DS silently became the TLS selector). `OS_VERSION` bumped to 38.24. |
| v38.23 | **UID/GID ownership + permission enforcement across VFS and ext2.** The OS had passwords but no file ownership — every Ring 3 app could read, write and delete anything. Every VFS node now stores a POSIX **uid/gid + 9-bit mode**, and the syscall layer enforces it: `SYS_OPEN` denies open-for-read/write when the caller's row lacks the bit, `do_sys_read`/`do_sys_write` re-check **per call** (so a `chmod` while a descriptor is open takes effect immediately), and create/mkdir/rename need write on the parent, delete needs write on the node, exec needs execute. Root (uid 0) bypasses everything; FAT32, `/proc` and `/dev` keep no permission concept (their on-disk format can't store an owner). New **`SYS_CHMOD` (102)** — owner or root may set the 9 bits — and **`SYS_CHOWN` (103)** — root-only, so a user can't give files away to dodge quotas. Ring 3 apps are loaded with uid 1000; the kernel task stays uid 0. Shell gained `chmod`/`chown` builtins and **`ls -l`** (mode string, owner, gid, size — e.g. `-rw-r--r-- 0 0 14 hello.txt`). Mode/uid/gid ride on the VFS node-table layout (bumped to v3, old images auto-rebuild) and sync to ext2 inodes via `ext2_write_inode`, so `chmod`/`chown` survive the ext2 image. Defaults keep the single-user OS usable: root dir and kernel-created dirs are 0777, `/apps/*` executables 0755, user files 0644, user dirs 0755. `apps/permtest.mct` proves the whole model from Ring 3 (create/write own file, chmod 0400 → write denied + read allowed, chmod 0000 → both opens denied and a pre-open fd also refuses read/write, chmod restore, chown denied for a non-root caller, root-owned 0644 file read-allowed/write+delete-denied) and `scripts/perm_test.py` runs it end-to-end in CI. `OS_VERSION` bumped to 38.23. |
| v38.22 | **Multi-core panic dump + `panic=reboot`.** A kernel panic previously printed a single EIP/CS line from the *faulting* core and halted — a deadlock or a 4-core race looked identical to a single-core fault. Every panic path (unhandled kernel exception, #DF, stack overflow, heap corruption) now funnels into `panic_finish()` → `panic_dump_all_cores()`: the panicking core snapshots itself, sends an **NMI IPI** to all other cores (NMI is never masked by IF, so even a core parked inside a spinlock records its registers), each core's NMI handler (vector 2) writes its state into a per-core slot and parks, and the BSP prints one full register line per core (`[PANIC] CPU n EIP=… CS=… ESP=… EFL=… EAX=… CR3=…`). New `src/sys/panic.c` uses only try-writes and raw stores — the panic path may already hold any lock, so it never takes one. Boot arg **`panic=reboot`** (parsed from the GRUB command line before paging) makes the tail do a 8042 keyboard-controller reset instead of halting — under QEMU with `-no-reboot` a panicking CI run now **exits cleanly** with a full multi-core dump in the log instead of hanging until the workflow timeout. Boot arg **`panic_self_test`** fires a deliberate panic once SMP is up, exercising the whole path end to end; `scripts/panic_test.py` builds a boot ISO with both args, boots with `-no-reboot`, and asserts all four `[PANIC] CPU 0..3` lines appear and QEMU exits — now a CI regression. `OS_VERSION` bumped to 38.22. |
| v38.21 | **CI smoke tests for every demo app + one-boot app runner.** The CI workflow previously covered only boot, fork/jobcontrol, FAT32, poll/select, fuzz and page cache — the remaining demo apps shipped on disk but were never verified automatically. New `scripts/app_smoke_test.py` boots once, opens the terminal, and runs three apps back-to-back with per-app markers: `smpstress` (8 children across the 4-core SMP scheduler — CPU burns, sleep slices, self-signals; parent reaps all with codes 101–108 → `[SMPSTRESS] ALL PASS`), `sigdemo` (sigprocmask hold/pending/unblock, sa_mask deferral, SA_NODEFER, SA_RESTART sleep resume → `[SIGDEMO] ALL TESTS PASSED`), and `crashme` (deliberate `ud2` → kernel logs `[EXCEPTION] int_no=6` and the OS survives — the harness asserts the marker *and* zero `[PANIC]` since crashme dies on purpose). The three existing per-app harnesses — `mmapfile_test.py` (lazy fault-in, msync flush, munmap), `rusthello_test.py` (first Rust Ring 3 app), and `lseekfile_test.py` (lseek/fstat/O_APPEND) — are now wired into CI as well, so every push runs all 13 suites. The stale `kvm_smp_test.py`/`kvm_cpumon_test.py` mentions (scripts that never existed) were dropped from the README. Workflow timeout raised 20 → 45 min. `OS_VERSION` bumped to 38.21. |
| v38.20 | **Whole-file page cache for MECTOVFS + same-size write fast path.** The VFS read model was one PIO transfer per 512-byte sector (and every write rewrote the 256-sector node table), so repeated reads of the same file — `cat`, `grep`, app relaunches, mmap fault-ins — paid the disk every time. New `src/sys/pcache.c` is a small LRU table (24 entries, files ≤ 128 KB) of whole-file RAM copies guarded by `pcache_lock`, ordered `vfs_lock > pcache_lock > ata_lock` (heap below) so the lock-free mmap fault path can take it safely. `vfs_read_file`/`vfs_read_file_offset` now check the cache before touching ATA, and a first read *primes* the whole file (with the same end-of-disk clamp as the raw path, so a corrupt node can't walk off the image). Coherence is write-through: `vfs_write_file_unlocked` refreshes the cached copy after the disk write, and delete/recursive-delete/node-slot-reuse invalidate. Same-size in-place rewrites now skip the 256-sector `vfs_save()` entirely (the persisted node record is unchanged), cutting the dominant cost of small file writes. `apps/iobench.mct` measures it from Ring 3: cold read of an 855-byte app file ≈ 7.4 ms on TCG, cached read ≈ 72 µs — ~100× faster; `scripts/iocache_test.py` boots it, asserts cached reads beat cold by 3x (min-of-N, immune to TCG's fixed syscall overhead), verifies zero `[PANIC]`, and is now a CI regression. Also closes the last two low-severity findings from the v38.19 locking audit: `task_signal_group()` (terminal Ctrl+C/Ctrl+Z) now snapshots the descendant set under `task_lock` before delivering — a concurrent fork/exit could previously reparent tasks mid-walk and signal the wrong set (delivery itself stays outside the lock because `task_signal()` re-takes it) — and `SYS_UPDATE_WINDOW` sets `wm_wins[idx].buffer_dirty` under `wm_lock` instead of writing the shared window table unlocked. `OS_VERSION` bumped to 38.20. |
| v38.19 | **Kernel audit fixes + Ring 3 syscall fuzzer.** A syscall-boundary audit found three untrusted-input bugs, all fixed. (1) **Array-count overflow in `SYS_LIST_DIR`/`SYS_GET_TASKS`**: `validate_user_ptr(array, sizeof(T) * max_count)` computed the size in 32-bit arithmetic, so an absurd `max_count` wrapped to a tiny value, validation passed, and the kernel then wrote up to 256 entries past the caller's mapped region at CPL 0 → kernel #PF/panic. Both now route through `validate_user_array_ptr()` (64-bit math, `count <= 0` / zero-size rejection, 16 MB cap) like every other array syscall — the `SYS_GET_WINDOWS`/`SYS_GET_PCI_INFO`/poll pattern that already neutralised this class. (2) **`SYS_SIGRETURN` restored CS/SS/EFLAGS from the user-writable sigframe**: a crafted frame with a non-Ring-3 CS made the final iret fault at CPL 0 (kernel panic) and NT in EFLAGS could arm a spurious hardware task switch; the sigreturn path now requires Ring-3 CS/SS selectors and masks NT + IOPL. (3) **`SYS_KILL` had no authorization** — any Ring 3 app could SIGKILL/SIGSTOP the shell, terminal or any other task; plain apps are now limited to self, own descendants, or same-pgrp targets (whitelisted terminal/explorer/taskmgr keep full access; kernel-internal kill/Ctrl+C paths unaffected). New **`apps/fuzz.mct`**: ~2000 random syscalls with garbage pointers (NULL, unmapped, kernel identity-map addresses), absurd sizes/counts and invalid fds/pids, plus targeted probes for the three fixes above; `scripts/fuzz_test.py` boots it in QEMU and asserts clean `[FUZZ] DONE` + zero `[PANIC]` — now a mandatory CI regression. `OS_VERSION` bumped to 38.19. |
| v38.18 | **Build fix: `doom_libc.h` was missing declarations that break the DOOM build on GCC 14+.** The DOOM tree compiles with `-w`, which on older GCC silently swallowed three implicit-declaration *warnings* — but GCC 14 promoted implicit function declarations to hard errors, so `make` died on `doom/i_system.c`, `m_config.c` and `v_video.c` the moment CI's runner upgraded its toolchain. All three functions were already defined in `doom_libc.c`, just never declared in the header: added `double atof(const char*)` and `int system(const char*)` to the stdlib section, and `double fabs(double)` to the math section. Verified: full `doom/*.c` syntax-check under GCC 15 is clean. `OS_VERSION` bumped to 38.18. |
| v38.17 | **Smooth-compositing overhaul: delta-copy VRAM presentation, truthful FPS HUD, tick-scaled 60 FPS cap.** Render profiling (`scripts/render_prof.py`) pinned the desktop's stutter on `swap_buffers()`: it memcpy'd the whole dirty rect to the framebuffer every frame, and the framebuffer is MMIO — under KVM every write is a VM exit, under TCG they're just slow. `swap_buffers()` now does the delta copy the design always intended (`docs/drivers/vga_vbe.md`): it compares the back buffer against the shadow copy of the last presented frame and writes only the *changed runs* to VRAM. Dragging a window changes mostly the leading/trailing strips (the overlap is identical), cutting VRAM traffic ~10x — measured drag render time on TCG dropped ~44 ms → ~10 ms and resize ~127 ms → ~9 ms; on KVM the desktop renders in well under 1 ms/frame. The hardware cursor lives only on VRAM, so the old cursor rect is now force-repainted from the back buffer before drawing the new one (otherwise delta-copy would skip it and leave a ghost cursor). The FPS HUD is now truthful: it counts cursor-only swaps too (previously a moving mouse still read "5 FPS"), converts the elapsed window by the *measured* tick rate (`fps = frames × ticks_per_sec / elapsed_ticks`) instead of assuming 1000 Hz, and on a static desktop it keeps the last reading instead of decaying to the HUD's own 200 ms cadence — and no longer forces a redraw just to refresh itself. The 60 FPS composition cap is scaled the same way: a raw 16-tick window at TCG's drifted 549 ticks/s capped the desktop at ~34 FPS; it's now a real 60 FPS in wall time (`ticks_per_sec × 16 / 1000`). All 7 GUI/regression tests still green (boot, fork, jobcontrol, lseekfile, mmapfile, rusthello, fat32). `OS_VERSION` bumped to 38.17. |
| v38.16 | **FAT32 long file names (LFN) — read AND write.** The v38.15 driver was 8.3-SFN-only: names from Windows/real tools came back truncated (`THEQUI~1.TXT`) and files created by the OS were SFN-only. Directory scanning now reassembles full names from the 0x0F LFN prefix entries — stored in reverse order (the entry farthest from the SFN holds the last chunk and carries the 0x40 chain-start bit), each verified against the FAT checksum of the 8.3 short name before being trusted, so mtools/Windows volumes read correctly regardless of their `~1`-style SFN generation. New files get an LFN chain whenever the name doesn't round-trip through its uppercase 8.3 form (Windows convention — lowercase like `write.txt` gets one too); the SFN uses the `first6~1.EXT` scheme. Lookup/update/remove/rename match by the full reassembled name (falling back to the SFN), and delete marks the whole LFN prefix + SFN run deleted. `apps/fat32demo.mct` now reads mtools-created LFN files (incl. a long-named nested dir `My Vacation Photos/summer2026 beach.txt`), creates a long-named file itself, and the test verifies with host mtools that the OS-written LFN entry is readable **by its full long name** — interop proven both ways. Also fixed two latent bugs surfaced by long paths: the directory scanner used a shared static buffer that recursive subdirectory population clobbered (entries after the first subdirectory were silently skipped — heap-buffered per scan now), and `SYS_OPEN`/`SYS_CREATE_FILE` capped the *whole path* at `MAX_FILENAME` (32) instead of `MAX_PATH` (256), so any path over 32 chars — e.g. `/fat32/demo/long file name test.txt` — was rejected. `OS_VERSION` bumped to 38.16. |
| v38.15 | **FAT32 read/write — mount, list, read, write, create/delete, integrated into the VFS.** Mectov could only use its own MECTOVFS (drive 0) and ext2 (drive 1); now a real FAT32 volume on drive 3 (secondary slave — the CD-ROM owns drive 2) mounts at `/fat32`. New `src/sys/fat32.c` parses the BPB (512-byte sectors, ≤16 sectors/cluster), walks FAT cluster chains, and reads/writes 8.3 SFN directory entries (LFN entries skipped on read; new files are created SFN-only). The ATA driver grew secondary-channel support (`0x170` port base for drives 2–3, master/slave by parity). VFS integration mirrors ext2 exactly: `FS_FAT32_FILE`/`FS_FAT32_DIR` node types dispatch to the driver from `vfs_read_file`/`vfs_write_file`/`vfs_create_node`/`vfs_delete_node`/`vfs_rename`; the first cluster of each object lives in the node's `data_sector`; the mount point is rebuilt from the disk every boot (`vfs_clear_children`) so an external volume swapped at runtime is never shadowed by stale persisted nodes. Because fd writes route through the whole-file `vfs_write_file`, lseek/O_APPEND work on FAT32 files for free. The disk image is a 16 MB FAT32 volume (mkfs.fat + mtools), and `df` gains a `fat32` row via `fat32_get_stats`. `apps/fat32demo.mct` proves it from Ring 3: reads mtools-created files (incl. a nested subdirectory), creates a directory + file, writes, reads back, appends via O_APPEND, and deletes — and the resulting volume is readable by real mtools on the host (verified: mtools lists the OS-created `DEMO` directory). New `scripts/fat32_test.py` runs it end-to-end in CI (drive attached at index=3, TCG). `OS_VERSION` bumped to 38.15. |
| v38.14 | **RTC-calibrated tick rate + CI regression fixes.** The GUI assumed a fixed 1000 Hz PIT tick rate, but under TCG/emulation the virtual clock drifts wildly (measured 549 Hz at boot, ~1.9 kHz under load, faster still on CI runners) — so the desktop double-click window of "800 ticks" was really ~0.1–0.3 s of real time and clicks always missed. `timer_calibrate_ticks_per_sec()` now counts PIT ticks across one full CMOS RTC second at boot, and a rolling per-second update in the main loop keeps `ticks_per_sec` tracking the live rate; the double-click window is computed as `ticks_per_sec × 0.8` — real 0.8 s regardless of clock speed, making double-clicks reliable for real users under slow emulation, not just tests. Also fixed a fresh-disk seeding bug: the VFS had two app-seeding paths and the fresh-disk one silently omitted `forkdemo.mct` (plus `volume.mct`/`mplayer.mct`/`music.wav`), so `run /apps/forkdemo.mct` failed on any new disk — including CI's — while existing disks worked. CI hardened: GUI tests share a `scripts/terminal_launch.py` helper that verifies the cursor is exactly on the Terminal icon via screendump before clicking (with cursor-position reporting on failure), the KVM step guards on `/dev/kvm` availability, and a failed boot dumps the serial tail. Result: the Build & Boot Test workflow is fully green (10/10 steps) for the first time since v38.8. `OS_VERSION` bumped to 38.14. |
| v38.13 | **First Ring 3 application written in Rust — `apps/rusthello.rs`.** Proves the `.mct` format is language-agnostic: a freestanding `no_std`/`no_main` Rust binary (entry `_start`, linked at `0x08000000`) is flattened with objcopy, wrapped in the standard MCT1 header, and runs through the exact same loader path as the C apps — no kernel changes. `scripts/build_rust_mct.py` mirrors `build_mct.py` using rustc targeting the built-in `i686-unknown-uefi` (the only freestanding 32-bit x86 target with a prebuilt core; its C ABI is cdecl; SSE/MMX disabled via `-C target-feature` so no FPU state escapes the kernel, which never saves it). The entry symbol resolves via `nm` (`__start` — that target decorates extern "C" symbols with a leading underscore). CI installs rustup + the UEFI target so the build stays green. `rusthello.mct` prints three markers to serial and exits 0 — verified under QEMU through the Terminal (`run /apps/rusthello.mct`); all regressions (boot/fork/jobcontrol/mmapfile/lseekfile/dhcp) green. `OS_VERSION` bumped to 38.13. |
| v38.12 | **POSIX file positioning & metadata — `SYS_LSEEK` (95) + `SYS_FSTAT` (96) + `O_APPEND`.** File descriptors now track a real read/write offset: `SYS_READ` reads at the descriptor's current position and advances it (POSIX sequential I/O), instead of always starting from byte 0. `SYS_LSEEK` repositions with `SEEK_SET`/`SEEK_CUR`/`SEEK_END` and returns the new offset (negative results and pipes → -1). `SYS_OPEN`'s mode argument is now honored — `O_APPEND` sends every write to the end of the file regardless of the offset. `SYS_FSTAT` fills a `stat_t {size, type, node_idx, parent, data_sector, name}` straight from the fd's VFS node — no path resolution, no rename race. The offset-aware reads reuse `vfs_read_file_offset()` (the unlocked reader built for mmap faults — only `ata_lock`, safe under `fd_lock`); dev/proc nodes keep the legacy whole-read path. `apps/lseekfiledemo.mct` proves it end-to-end: SEEK_SET/END/CUR reads at the right offsets, EOF read = 0, fstat size/type, O_APPEND grows the file from 27→31 bytes at the tail, in-place overwrite via lseek, and lseek-on-pipe/bad-whence → -1. `OS_VERSION` bumped to 38.12. |
| v38.11 | **DHCP client — IP/gateway/DNS configured at runtime over UDP broadcast.** The network config (`my_ip`/`gateway_ip`/DNS server/netmask) was hardcoded to the QEMU slirp layout; now `net_init()` runs a full RFC 2131 client: DISCOVER → OFFER → REQUEST → ACK, all broadcast (`0.0.0.0:68 → 255.255.255.255:67` to Ethernet `FF:FF:FF:FF:FF:FF`) via a new `net_send_udp_raw()` that bypasses the `net_ready` gate (normal UDP/TCP sends wait for the gateway MAC). Pre-bind the RX path accepts broadcast and `0.0.0.0` destinations (the client sets the broadcast flag), and the ACK handler overwrites `my_ip`, `gateway_ip`, `dns_server_ip` (option 6) and a new `netmask_ip` (option 1) in place, then re-resolves the gateway MAC — the ARP reply is what flips `net_ready` and dispatches queued DNS/TCP ops, so the whole stack works unchanged off DHCP. The DNS query path now targets `dns_server_ip` instead of a hardcoded address. The state machine is driven from `net_poll()` inside its cli window (1 s retries × 3, NAK restarts discovery); if no server answers, it falls back to the static defaults after ~4 s — networking comes up exactly as before on serverless links. New `ipconfig` shell command prints the runtime config (IP/netmask/gateway/DNS, DHCP-bound vs static, link state). KVM+slirp-verified: DISCOVER→OFFER→REQUEST→ACK→bound 10.0.2.15 gw=10.0.2.2 dns=10.0.2.3, then `net_ready` via ARP; all -net none regressions (boot/fork/jobcontrol/mmapfile) green. `OS_VERSION` bumped to 38.11. |
| v38.10 | **File-backed mmap — `SYS_MMAP_FILE` (93) + `SYS_MSYNC` (94).** `mmap()` could only reserve anonymous VA ranges; now an open VFS file fd maps into the mmap window and its pages are demand-paged *from the disk* on first access — the `#PF` handler reads each page's sectors straight off the ATA drive via a new unlocked offset-aware reader (`vfs_read_file_offset`, takes only `ata_lock`), because a user fault can fire while the same CPU already holds `vfs_lock` (e.g. `SYS_READ` copying into an mmap'd buffer) and taking it again would self-deadlock. File pages fault in read-only; the first write faults again (RO→RW upgrade), marks the page dirty in a per-region kmalloc'd bitmap, and `SYS_MSYNC`/`SYS_MUNMAP` flush dirty pages back to the file (whole-file RMW bounded by the file's current size — writes past EOF are dropped, POSIX-style, so the mapping never grows the file). File-backed PTEs carry `PAGE_SHARED`, so `fork()` leaves them shared between parent and child — true MAP_SHARED semantics — while each task deep-copies its own dirty bitmap; exec/exit discard mappings without writeback (POSIX-style). The mapping records the VFS node itself, so closing the fd after mmap is legal. `apps/mmapfiledemo.mct` proves it end-to-end: create+seed file → mmap → lazy fault-in from disk → in-place write → msync → re-read shows the change on disk → munmap. `OS_VERSION` bumped to 38.10. |
| v38.9 | **Single-consumer keyboard — SMP input race fixed + real foreground-app key buffer.** The PS/2 scancode ring (`kbd_buffer`) used to have TWO consumers — the main loop (task 0) and `SYS_GET_KEY` — that stole scancodes from each other nondeterministically on 4-core KVM (CI's TCG runs never reproduced it), so keystrokes vanished between the main loop and apps and the terminal sometimes never received typed input at all. Now the main loop is the ONLY reader: it resolves each key with the feed-time modifier snapshot and forwards to the focused window as before, or queues it to a NEW real foreground-app buffer when `term_app_running` (previously `term_app_push_key`/`term_app_pop_key` were stubs returning 0, so interactive apps like `calc`/`echo_test` could never read input). `SYS_GET_KEY` pops that buffer — a background task still gets SIGTTIN, anything else gets 0, and the buffer is cleared on app launch/exit/Ctrl+Z so stale keys never leak. Also made the `[TASK] fork:` marker a single locked write (the COW #PF handler's raw fallback was interleaving bytes into it under real SMP) and added `write_serial_if_free()` for routine per-fault diagnostics so they skip instead of garbling another core's locked line. KVM-verified: `fork_test --kvm` (4-core) passes deterministically 2× — now a mandatory CI regression — plus new `apps/keyshow.mct` proving a Ring 3 app receives typed keys via `SYS_GET_KEY` (`KEY=x` on serial); boot/fork/jobcontrol all green; calc runs interactively in the terminal. |
| v38.8 | **`dmesg` — kernel log ring buffer + `/proc/dmesg` + shell command.** Every byte the kernel writes to the serial port is now captured in a 64 KB in-memory ring (`src/sys/klog.c`) via both serial putc paths, including the exception path (`write_serial_try`), so crash/panic lines are queryable in-guest instead of only on the serial console. Locking is deliberate: writers take no lock (the exception path must be able to log even mid-`klog_snapshot` — the same deadlock `write_serial_try` avoids by try-locking), readers take `klog_lock` with interrupts disabled. Shell command `dmesg` (8 KB heap-buffered chunk, not the 16 KB kernel stack) and `/proc/dmesg` expose the newest `KLOG_SIZE` bytes with tail semantics (a small read shows the most recent activity, not the boot prefix). Bonus VFS fix: new `/proc` nodes previously never appeared on existing disks because the directory already existed and creation was gated on `/proc` missing — now missing nodes are added to an existing `/proc`, so an upgraded disk gains `dmesg` without a rebuild. KVM-verified: `dmesg` prints ~3.5 KB of boot log; `cat /proc/dmesg` shows VFS/tasking/BOOTED markers; ring length tracks serial output exactly over time (probe 5×); crashme's `[CRASH]` line (exception path) is present in the ring buffer; OS alive throughout; fork 6/6, jobcontrol 6/6, boot regressions green. |
| v38.7 | **SIGSEGV delivered to user handlers on unresolvable Ring 3 faults.** A user #PF/#GP/#UD that demand paging / COW cannot resolve used to kill the task immediately; now it routes through the normal signal path — a task that installed a handler (sigaction/sys_signal) gets its sigframe pushed and EIP/USERESP rewritten so the handler runs, and the faulting instruction is re-executed on return (a handler that fixes the cause — or exits — makes progress; otherwise SIGSEGV re-delivers). Default action (no handler; SIG_IGN on a fault is treated as kill) terminates with exit status `128+SIGSEGV` = 139 after full cleanup (WM + VMM + fds), and the frame is parked so a dead task's user context is never iret'd. `apps/segvtest.mct` exercises both modes: NULL dereference without a handler → task ZOMBIE with exit_code=139 (verified by reading kernel memory), with `catch` → handler runs twice (delivery + re-delivery) and exits cleanly. Verified deterministic 2×; fork/jobcontrol/smpstress/boot regressions green. |
| v38.6 | **Lock audit, heap hardening, unified physical allocator, keyboard shift fix.** Global `cli()` in syscall paths replaced with irqsave spinlocks per structure (VFS, fd table, WM windows, IPC queues, task runqueues) so syscalls are preemptible and SMP actually parallelizes; the audit caught two real bugs — `gui_unlock`'s unconditional `sti` inside a held `wm_lock` (deadlock: main loop preempted while holding the lock, terminal spun forever) and a background child inheriting the shell lock then releasing it on exit (blocking every later command). Heap hardening in `mem.c`: every `kmalloc` block gets a magic + canary, `kfree` panics cleanly on non-allocated/double-free, overflow detection via redzone, OOM returns NULL instead of hanging, and allocator stats land in `/proc/meminfo`. Physical allocator unified: the hardcoded 128 MB frame bitmap in `vmm.c` and the dead multiboot bitmap in `mem.c` became one source of truth driven by detected RAM (identity map to 512 MB, framebuffer reserved) — verified with `-m 256`: `MemTotal: 262016 KB`, frames above 128 MB allocated and usable. Keyboard: per-byte modifier snapshot fixes shift loss (shift-7 → `&`) when the whole scancode sequence drains in one IRQ. |
| v38.5 | **COW fork + lazy zero page + demand paging for heap and stack.** One shared zero frame (refcount-pinned) backs every untouched heap/stack page: a read fault maps it read-only (COW marker), the first write duplicates it into a private frame — a process that mallocs a lot but writes little costs almost nothing. User stacks are now demand-paged per task (`[TOP-(tid+2)·64KB, TOP-(tid+1)·64KB)`), with the page below the range left unmapped as a guard page: a genuine stack overflow faults there and kills the task cleanly instead of corrupting memory (verified by deliberately overflowing a 64 KB stack). `apps/demandtest.mct` proves heap zero-fill, write/readback, 100-level recursion growing the stack on demand, and COW fork isolation — no #PF ever leaks to user space; 2× deterministic + full regressions green. |
| v38.4 | **Unified physical allocator (RAM > 128 MB).** The frame bitmap in `vmm.c` was hardcoded to 128 MB while `mem.c` carried a multiboot-derived bitmap that nothing read — RAM above 128 MB was simply unusable. Now one allocator owns the truth: `phys_init(total_pages)` sizes the bitmap from the multiboot memory map, `frame_alloc`/`frame_free`/`zero_phys_page` respect the dynamic ceiling, `phys_reserve_region()` keeps the framebuffer/MMIO out of the free pool, `get_used_memory`/`get_free_memory` report real numbers from the bitmap, and the identity map covers 512 MB. Verified under QEMU `-m 256`: allocator sees `0x0FFE0000`, `MemFree: 212452 KB` (~207 MB free vs ~80 MB with the old cap), and a probe allocated 128 MB of frames past the 128 MB mark, wrote/read a pattern, and freed them all. |
| v38.3 | **Kernel hardening: stack guard pages with clean panic, shell split into modules, stack watermark.** Kernel stacks moved out of `task_t` into a page-aligned arena — a 4 KB unmapped guard page below each 16 KB stack, so an overflow now #PFs on the guard page and panics with a clear message instead of silently corrupting memory. Because an overflow can strike while the CPU is mid-push (the #PF frame itself can't be saved), vector 8 became a hardware task gate with its own TSS/stack/CR3: worst-case overflow prints a full `[PANIC] DOUBLE FAULT` (CPU, EIP, ESP, CR3) and halts instead of triple-faulting into a silent reboot. Exception handlers run on per-CPU fault stacks with nested-fault detection, and logging is deadlock-free (`spin_try_lock` + `write_serial_try`) so a 4-core race can't freeze the panic path. Surfaced and fixed two real bugs: a fork frame offset bug (child could iret with cs=0 → #GP) and a shared fault stack clobbered by simultaneous COW faults. The 3,384-line `src/sys/shell.c` monolith was split into `src/sys/shell/` — dispatcher core + 72 per-command files + `job/` + `script/` — extracted mechanically with token-identical reconstruction and A/B-verified against the old build; all toolkit tests byte-identical to baseline. New stack watermark: the scheduler records each task's peak kernel-stack depth at every preemption, and `/proc/tasks` gains a `STK%` column (peak bytes / 16 KB) — verified live: terminal 1% idle, 16% after a blocking `sleep 2`. `OS_VERSION` bumped to 38.3. |
| v38.2 | **Password stored in /etc/passwd + `passwd` command.** The login password is no longer hardcoded: it lives in `/etc/passwd` (MECTOVFS), with `mectov123` kept only as a fallback for a fresh disk where the file doesn't exist yet. New `src/sys/passwd.c` exposes `sys_get_password`/`sys_set_password` — both the login screen and the shell go through them, so the default lives in exactly one place. Shell builtin `passwd <current> <new>` verifies the current password (against the file or the fallback) before writing, creating `/etc` and the file on first use; it's in tab completion, `help` and `man`. KVM-verified end-to-end across three boots: fallback login on fresh disk, wrong-current-password writes nothing (checked by parsing the raw disk image), change persists across reboot, old password rejected / new one accepted. `OS_VERSION` bumped to 38.2. |
| v38.1 | **Windows-style lock screen + PS/2 init race fix.** Login flow split into two stages like Windows 10: a minimal lock screen with just a large 8× scaled live clock (blinking colon), date, small wordmark and a dismiss hint — any keypress or click dismisses it (the dismissing press is consumed, so it never leaks into the password field; the dismissing click is debounced 300 ms so it can't instantly hit Sign In) — then the existing amber instrument panel for password entry with the clock/date shrunk to the top-left. Clock rendering rewritten: the old `draw_char_scale` advance bug (return value assigned instead of accumulated) stacked every glyph after the first at x=64, rendering the big clock as one digit plus a solid amber blob — fixed with `gx +=`. Also fixes a PS/2 init race: IRQ1/IRQ12 firing mid-protocol could steal the controller's command-byte reply (e.g. `0x41`) into the keyboard buffer as a bogus scancode, instantly dismissing the lock screen at boot — the whole `init_mouse` protocol now runs with interrupts disabled (eflags preserved). KVM-verified: lock screen layout (pixel audit), space-dismiss, click-dismiss, wrong-password stays, correct login → desktop; ABC regression 10/10, toolkit round 3 green. |
| v38.0 | **Amber GUI identity + critical stack-overflow fix.** Window chrome (`TOARU_*` in `theme.h`) and taskbar (`TB_*`/`RETRO_*`, start-logo accent) unified from neutral gray to the amber palette introduced by the login redesign. Fixes a deterministic #PF: v37.9's `sort`/`uniq` had `fbuf[4096]` + `lines[256]` as `run_cmd_internal` stack locals, pushing its frame to 6.3 KB — combined with the `load_mct_app → vmm_create_address_space` chain it overflowed the 16 KB kernel stack when launching any app after using sort. Buffers moved to `kmalloc`/`kfree` (frame back to 2.4 KB); regression suite 10/10, toolkit + chrome KVM tests green. |
| v37.9 | **Shell toolkit round 3:** `printf FORMAT [args]` (`%s`/`%d`/`%x`/`%c`/`%%`, `\n`/`\t` escapes, unsigned `%x` parse, INT_MIN-safe `%d`), `sort [file]` (stdin via pipe/redirect, insertion sort), `uniq [-c] [file]` (consecutive-duplicate collapse with optional counts), `tee FILE` (stdin → file + stdout), `find [dir] [-name GLOB]` (flat-node-table tree walk via parent chains, `*`/`?` glob). Linear-time greedy glob matcher (the recursive backtracking version was exponential on `*a*a*…*b` patterns). All five KVM-verified end-to-end; helper unit tests 24/24 (empty lines, CRLF, pathological glob). |
| v37.8 | **Instrument-console login + VFS layout constants + RTC fix:** graphical login redesigned from the generic glassmorphism template to a machine console — warm charcoal + phosphor amber palette, a live CMOS RTC clock rendered at 2× bitmap scale (the RTC read itself is gated to ≤2/s so the UIP busy-wait can't stall a frame; previously re-read every 16 ms frame), and a real system manifest (core blocks from `smp_cpu_count`, live uptime). Hit-test/render geometry unified under one `LOGIN_PW/PH/PY` define (the click region had drifted from the drawn button); actionable error copy; footer composes from `OS_VERSION` (bumped 37.0 → 37.8). VFS on-disk layout constants moved to `vfs.h` as a single source of truth — shell `df` now counts the 256-sector node table (was hardcoded `65`, under-reporting usage since the 64→256 node migration) and the allocator/read clamps use `VFS_DISK_SECTORS`. winman test app drops its redundant per-window hex echo (the kernel `[WM] create wid=` mirror is authoritative). Doom auto-deps completed (`-MMD -MP` in DOOM_CFLAGS). KVM-verified: login → desktop, wrong-password shake + error copy, pixel audit of clock/wordmark/manifest, ABC regression 10/10, window capacity 5/5. |
| v37.7 | **Exception stubs (no silent reboot), VFS 256 nodes, WM 16 windows, soft-float:** every real fault vector gets a dedicated stub so a user fault (#UD, #GP, #PF…) prints the exception + `int_no` and kills only the faulting task instead of rebooting the kernel (crashme demo app exercises #UD); MECTOVFS node table grows 64 → 256 (on-disk layout v1 → v2, old images rejected and rebuilt from embedded apps); window manager capacity 8 → 16 (desktop apps can now open up to 13 windows); kernel compiled with soft-float (`-msoft-float -mno-80387`) with a software fmod fallback for Doom. |
| v37.6 | **Ctrl+C interrupts the foreground app, Ctrl+Shift+C copies:** POSIX-style SIGINT delivered to the foreground process group from the Ring-3 terminal (interrupts `looper`/`snake`, etc.), while Ctrl+Shift+C keeps the clipboard-copy binding — the two are now distinct. Kernel-side `SYS_EXIT_GROUP`-style signal dispatch + terminal event plumbing. |
| v37.5 | **Shell toolkit round 2:** `wc` (words/lines/bytes), `type` (builtin / alias / external classification), `cd -` (OLDPWD), `cat -n` (line numbers), `yes [string]` — all shell builtins with pipe support, KVM-verified. |
| v37.4 | **DOOM silent by default:** SB16 DMA/IRQ activity while Doom streams audio stalled the game loop (window freeze), so sound is now opt-in (`doom -sound`); without it the SB16 module is never touched and Doom runs clean. Also fixed the broken doom_libc `vsnprintf` (integer overflow). run.sh gains audio backend auto-fallback (pipewire > pulseaudio) and drops the flaky pcspk audio device. |
| v37.3 | **Doom freeze diagnostics:** serial heartbeat (`[DOOM] tick`) + on-screen frame counter so a frozen window can be told apart from a dead OS; `doom -nosound` escape hatch; traced the freeze to SB16 DMA/IRQ during streaming (→ fixed properly in v37.4). |
| v37.2 | **Doom sound attempt:** enabled `FEATURE_SOUND`, unmasked SB16 IRQ5, wired the DSP IRQ path — exposed the DMA/IRQ freeze that v37.3 diagnosed and v37.4 fixed. |
| v37.1 | **Shell scripting:** `for VAR in list` + `while true/false` loops with `$VAR` expansion, `break`, nested loops (4 levels), all state on the script interpreter's own stack (re-entrancy fix: nested `run` inside a loop no longer clobbers loop state). KVM-verified step by step. |
| v37.0 | **Virtual /proc filesystem:** `/proc/tasks`, `/proc/meminfo`, `/proc/cpuinfo`, `/proc/uptime`, `/proc/version` — generated on read from live kernel state via `FS_PROC` nodes; KVM-verified each file. |
| v36.9 | **Shell standard toolkit:** six POSIX-style builtins — `uname` (with `-a` kernel banner), `whoami`, `hostname`, `env` (lists exported vars), `seq [FIRST] LAST`, `head [-n N] [file\|stdin]` with a serial mirror so truncation/pipes are testable; single `OS_VERSION` constant shared by help/mfetch/uname (was stale at v35.2); env/alias tables lazy-init in the Ring-3 terminal path under one shared module-level guard (defaults like `$USER` previously missing there); `head -n` dispatch, tab completion, help and `man` pages updated. KVM-verified: all six commands, `-n` truncation, and `cat f \| head -n 1` pipes. |
| v36.8 | **Per-CPU load monitor:** scheduler samples each core's utilization every tick (50 ms window) and exposes it via `SYS_GET_SYSINFO` (`cpu_count`, `cpu_load[4]`); SysInfo renders four live per-core bars; serial `[LOAD]` line every 3 s; SysInfo sleeps between frames instead of spinning. |
| v36.7 | **Per-CPU SMP scheduler:** per-core runqueues with priority+aging pick and work-stealing migration; pinned Ring 0 idle task per AP; LAPIC timer per AP (PIT-calibrated once on the BSP — concurrent calibration caused timer storms); one cli-first `task_lock` for every runqueue mutation; locked signal delivery on the syscall-return path; SMP-safe serial (locked buffer writes). Fixed along the way: missing `spin_unlock` in `task_signal` (froze OS at `waitpid`), phantom-CPU scan in `rq_least_loaded`, cross-CPU page-dir free race. `smpstress` passes 16/16 children on 4 cores. |
| v36.6 | **Process groups, sessions & controlling terminal:** `SYS_SETPGID/GETPGRP/SETSID/TCSETPGRP/TCGETPGRP` (88–92); per-task `pgrp`/`session`; the terminal becomes session leader and claims the foreground group; `SYS_GET_KEY` delivers SIGTTIN to background readers (bgread demo); Ctrl+C/Ctrl+Z target the foreground process group. |
| v36.5 | **Signal hardening:** `SYS_SIGACTION`/`SYS_SIGPROCMASK` (86–87); blocked signals stay pending, SIGKILL/SIGSTOP/SIGCONT bypass the mask; handler delivery auto-blocks per `sa_mask` (unless SA_NODEFER); `SA_RESTART` re-parks interrupted sleeps; fork copies masks, exec preserves them. |
| v36.4 | **Real pipelines, app redirection & TCP server:** `SYS_DUP2` (84) + per-task fd inheritance; `task_fork_exec` spawns children directly into apps with rewired stdin/stdout; `run A \| run B`, `run A > file`, `< file`; job control completed (`jobs`/`bg`/`fg`/SIGTSTP); `SYS_TCP_LISTEN` (85) + echo server verified end-to-end. |
| v36.3 | **mmap + Ctrl+C:** `SYS_MMAP/MUNMAP` (82–83) demand paging; Ctrl+C sends SIGINT to the whole foreground job tree. |
| v36.2 | **exec, shm & trap-gate syscalls:** `SYS_EXEC` (77) via live-frame patching; System V shared memory (78–81); `int 0x80` switched to a trap gate (preemptible syscalls). |
| v36.1 | **KVM fork-GPF fix:** child frame copied from `TSS.esp0 - sizeof(registers_t)` instead of the possibly-stale saved ESP. |
| v35.5 | **Multi-connection TCP:** 8-slot conn table, per-conn seq/ack, retransmit/timeout sweeps, FIN handshake; `SYS_TCP_CLOSE` (70); RTL8139 SMP TX/RX race fixed with a spinlock. |
| v35.4 | **Security hardening:** ELF loader integer-wrap fix, script-recursion guard, VFS node sanitization, ext2 superblock hardening, fd lifecycle fix, precise `task_sleep()`, GUI/clipboard bounds. |
| v35.3 | **English UI localization:** shell commands, help, apps translated; legacy Indonesian aliases kept. |
| v35.2 | **Shell file management:** `cp`, `mv`, `rmdir`, `df`; ext2 unlink/delete consistency fixes. |
| v35.1 | **Ext2 write support:** full on-disk write layer on the secondary partition, verified with `fsck.ext2`. |
| v35.0 | **SMP, GDB, ELF & sync:** fixed ACPI/MADT parsing (all 4 cores boot), in-kernel GDB stub, ELF32 loader, semaphores/futexes, IRQ-driven networking + UDP API, CI boot test. |
| v34.x | Scheduler aging overhaul + VFS reclamation; memory/syscall hardening (kmalloc overflow checks, fd cap); kernel-ownership teardown cleanup; real clip-rectangle enforcement, single-owner minimize/restore, unified `ps2_drain()`, kill-free shell waits. |
| v33.x | Start-menu ghosting fix; multi-core SMP bring-up (INIT-SIPI-SIPI, per-core GDT/TSS/IDT, LAPIC/IOAPIC, MADT ISO routing). |
| v32.0 | Syscall modularization; heap/VMM memory-layout safety fixes. |
| v31.0 | Event-driven rendering (idle CPU 0%), 64-bit delta compositing, `SYS_UPDATE_WINDOW` redraw triggers. |
| v30.x | Alt+Tab switcher; English localization; kernel clipboard + Explorer CRUD + context menus. |
| v29.0 | File associations + Explorer double-click; shell argument parsing for `run`. |
| v28.0 | DOOM memory protection (heap at 24 MB), HUD font precision, fullscreen-reset crash fix. |
| v27.x | Per-task working directories, absolute-path launchers, `uptime`/`memstat`, path sanitization, notepad shortcuts, heap/VMM separation fixes. |
| v27.0–v26.0 | TCP redirection through the web gateway; COW paging; integrated nano editor. |
| v25.x | Scripts + env vars + aliasing; IntelliMouse scroll; SB16 audio; shared-library runtime; Gitea remote. |
| v24.0 | **DOOM engine port** (full game in the kernel). |
| v23.0 | Shadow framebuffer delta rendering, zombie auto-kill, Ctrl+C, snake rewrite, power-menu fixes. |
| v22.0 | VMM (per-process address spaces), IPC queues, 4-level priority scheduler, 14 new syscalls. |
| v21.0 | Premium UI: hi-res cursor, WiFi indicator, 60 Hz loop. |
| v20.0–v18.0 | Squircle icons + macOS buttons; glass-morphism UI; `.mct` app ecosystem + Ring 3 syscalls. |
| v17.0 | Terminus Bold font, draggable icons, VFS persistence. |

---

## License

GNU General Public License v2.0 (GPLv2). Created by **M Alif Fadlan**.

Kamu bebas menggunakan, memodifikasi, dan mendistribusikan project ini selama turunan yang kamu distribusikan juga dilisensikan GPLv2 dan menyertakan source code-nya. Karena kernel ini terhubung langsung dengan source code DOOM (juga GPLv2), seluruh work gabungan dilisensikan GPLv2.
